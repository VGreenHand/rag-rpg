# 项目设计问题诊断与方法论

> **Workspace:** d:\Study\Project\rag-rpg

---

## User
针对当前项目进行全面的项目设计问题诊断与分析，总结因采用"普通小脚本"开发模式、问题驱动式解决方法、缺乏明确预期结果及系统性项目设计规划所导致的具体问题表现。基于诊断结果，提供一套结构化的项目设计方法论与实践指南，明确下次项目开发前应重点关注的关键方向，包括但不限于需求分析与文档化、系统架构设计、开发流程规范制定、风险评估与应对策略、以及明确的项目预期成果定义等内容。

## Assistant
以下是对 rag-rpg 项目的全面设计诊断与系统化方法论指南。

---

# RAG-RPG 项目设计诊断报告

## 一、项目现状概述

经全面审查代码库，该项目定位为"面向 SillyTavern 的 RAG 记忆引擎"，核心功能包括对话记忆持久化、语义检索、剧情约束生成、断点续执行。代码库共计 25+ 个 Python 文件，涵盖 6 个核心模块、14 个管理脚本、2 个测试文件、1 个前端扩展。从功能完整度看项目是可运行的，但从工程设计角度看存在大量系统性问题。

---

## 二、"小脚本开发模式"的具体问题表现

### 2.1 架构层面：无分层结构，模块职责交叉

**表现：** 6 个核心模块全部平铺在项目根目录下，不存在 `src/` 或 `core/` 等包结构。`scripts/` 目录下的 14 个脚本与核心代码之间没有清晰的边界。

**具体证据：**
- [ingest_initial.py](file:///d:/Study/Project/rag-rpg/scripts/ingest_initial.py) 与 [ingest_new.py](file:///d:/Study/Project/rag-rpg/scripts/ingest_new.py) 各自独立加载 SentenceTransformer 模型、独立连接 ChromaDB，完全绕过了 [pipeline.py](file:///d:/Study/Project/rag-rpg/pipeline.py) 中已有的管道逻辑。这意味着同样的处理逻辑被复制了至少 3 份。
- [quick_search.py](file:///d:/Study/Project/rag-rpg/scripts/quick_search.py) 同样独立加载模型和数据库，未复用 [query_engine.py](file:///d:/Study/Project/rag-rpg/query_engine.py) 中的查询逻辑。
- 核心模块间没有定义接口/抽象类，[pipeline.py](file:///d:/Study/Project/rag-rpg/pipeline.py) 直接操作 ChromaDB，[query_engine.py](file:///d:/Study/Project/rag-rpg/query_engine.py) 也直接操作 ChromaDB，[constraint_engine.py](file:///d:/Study/Project/rag-rpg/constraint_engine.py) 同样直接操作 ChromaDB——三个模块各自维护数据库连接，没有统一的数据访问层。

### 2.2 代码复用：模板代码（Boilerplate）遍地重复

**表现：** 相同的基础设施代码在多个文件中反复出现，而非抽取为公共模块。

**具体证据：**
- `SafeTimer` 超时保护类完整复制了两份：[pipeline.py 第 47-63 行](file:///d:/Study/Project/rag-rpg/pipeline.py#L47-L63) 和 [query_engine.py 第 26-43 行](file:///d:/Study/Project/rag-rpg/query_engine.py#L26-L43) 的代码几乎完全一致。
- `sys.path.insert(0, ...)` 这个路径 hack 出现在至少 7 个脚本中（`check_prof.py`、`quick_search.py`、`setup_test_env.py`、`validate_character_consistency.py`、`test_suite.py`、`test_checkpoint_resume.py` 等）。每个脚本都要自己处理导入路径。
- `chromadb.PersistentClient(path=...)` + `get_or_create_collection(...)` 这个初始化序列重复出现在 5 个以上的文件中，每次都要重复指定 `metadata={"hnsw:space": "cosine"}`。
- 单例模式（`get_xxx()` 函数 + `_xxx_instances` 字典）在 4 个模块中各自独立实现，模式完全相同但代码重复。

### 2.3 脚本膨胀：14 个管理脚本无组织

**表现：** 脚本数量多且功能重叠，缺乏统一的入口和组织结构。

**具体问题：**
- [check_prof.py](file:///d:/Study/Project/rag-rpg/scripts/check_prof.py)（11 行）：检查熟练度用的临时脚本
- [check_keys.py](file:///d:/Study/Project/rag-rpg/scripts/check_keys.py)：检查 ChromaDB 与 JSON 的 key 一致性
- [check_metadata.py](file:///d:/Study/Project/rag-rpg/scripts/check_metadata.py)：查看条目元数据
- [check_env.py](file:///d:/Study/Project/rag-rpg/scripts/check_env.py)：环境检查
- [data_manifest.py](file:///d:/Study/Project/rag-rpg/scripts/data_manifest.py)：数据传输校验
- [clean_all_data.py](file:///d:/Study/Project/rag-rpg/scripts/clean_all_data.py)：清理数据

其中 `check_*` 开头的脚本有 4 个，功能高度重叠。这些脚本本应合并为一个 CLI 工具（如 `python -m tools check --type env`），但现状是每个需求来了就新建一个 `.py` 文件——这是典型的"问题驱动式开发"症状。

### 2.4 配置散落：硬编码与配置文件并存

**表现：** 虽然有一个 [config.py](file:///d:/Study/Project/rag-rpg/config.py)，但很多关键参数仍然硬编码在各个文件中。

**具体证据：**
- [query_engine.py 第 20-22 行](file:///d:/Study/Project/rag-rpg/query_engine.py#L20-L22)：`QUERY_TIMEOUT = 6.0`、`ENCODE_TIMEOUT = 8.0`、`COLLECTION_GET_TIMEOUT = 4.0` 定义在模块级别而非 config.py 中。
- [pipeline.py 第 26-30 行](file:///d:/Study/Project/rag-rpg/pipeline.py#L26-L30)：`EMBED_TIMEOUT = 15.0`、`CHROMA_TIMEOUT = 8.0` 等同样分散定义。
- [ingest_initial.py 第 9-12 行](file:///d:/Study/Project/rag-rpg/scripts/ingest_initial.py#L9-L12)：`JSON_FILE`、`COLLECTION_NAME`、`MODEL_NAME` 直接在脚本顶部硬编码。
- [ingest_new.py 第 9-12 行](file:///d:/Study/Project/rag-rpg/scripts/ingest_new.py#L9-L12)：同样的问题。

### 2.5 导入方式：sys.path hack 反模式

**表现：** 多个文件和脚本通过修改 `sys.path` 来导入父级模块，这是临时脚本的典型特征。

```python
# 出现在至少 7 个文件中
sys.path.insert(0, str(Path(__file__).parent.parent))
```

这不仅脆弱（依赖于文件在磁盘上的相对位置），还意味着这些文件无法作为安装包的一部分正常工作。如果项目被 pip install 安装，这些导入会全部失效。

### 2.6 测试体系：自建框架而非标准工具

**表现：** 测试没有使用 pytest，而是手写了一个简陋的测试框架。

**具体证据：**
- [test_suite.py](file:///d:/Study/Project/rag-rpg/tests/test_suite.py) 和 [test_checkpoint_resume.py](file:///d:/Study/Project/rag-rpg/tests/test_checkpoint_resume.py) 都使用全局变量 `TOTAL, PASSED, FAILED` 和自定义的 `check()` 函数来跟踪测试结果。
- 没有测试发现机制（discovery），测试必须手动编排执行顺序。
- 没有 fixtures、参数化测试、mock 等标准测试设施。
- 测试运行说明要求使用 `test_env/Scripts/python.exe` 而非当前环境的 Python——这说明测试环境是独立创建的，难以复现。
- 测试套件是单文件 700+ 行的庞然大物，违反了单一职责原则。

### 2.7 包管理：缺少项目元数据

**表现：** 没有 `pyproject.toml` 或 `setup.py`，项目无法作为标准 Python 包安装。

**具体证据：**
- `requirements.txt` 只有 5 行，使用宽松的 `>=` 版本约束（如 `chromadb>=0.4.0`），没有锁定版本。
- 没有定义入口点（console_scripts），所有脚本必须通过 `python scripts/xxx.py` 运行。
- 没有项目版本号（`server.py` 中硬编码了 `"2.0.0"` 字符串，但没有统一的版本管理）。
- 没有 `__init__.py` 定义包的公开 API。

### 2.8 模块间耦合：紧耦合与循环依赖风险

**表现：** 模块之间依赖关系混乱，缺乏分层。

**具体分析：**
- [server.py](file:///d:/Study/Project/rag-rpg/server.py) 同时依赖 `pipeline`、`query_engine`、`constraint_engine`、`checkpoint_manager` 四个模块，且没有依赖注入，全部通过模块级单例函数获取。
- [pipeline.py](file:///d:/Study/Project/rag-rpg/pipeline.py) 直接依赖 `embedding_client` 和 `chromadb`，同时也依赖 `checkpoint_manager`。
- [query_engine.py](file:///d:/Study/Project/rag-rpg/query_engine.py) 同样直接依赖 `embedding_client` 和 `chromadb`。
- [constraint_engine.py](file:///d:/Study/Project/rag-rpg/constraint_engine.py) 中 `load_skills_from_db()` 方法内部 `import chromadb`，这是一个延迟导入（late import）——通常用于解决循环依赖，但这恰恰说明设计有问题。

换言之，项目目前的依赖图接近于"所有模块依赖所有其他模块"的混乱状态。

---

## 三、问题根源分析

### 3.1 问题驱动式开发（Problem-Driven Development）

这是最核心的根源。项目的演进模式是：**遇到一个问题 → 写一个脚本解决它 → 下一个问题 → 再写一个脚本**。具体表现：

- **没有需求文档**：`README.md` 虽然有功能描述，但缺少对系统边界、性能指标、用户场景的精确定义。
- **没有架构设计**：所有模块都是"自然生长"出来的，而不是经过设计决策的。例如，`embedding_service.py`（独立进程嵌入服务）是在遇到"主服务重启需重载模型太慢"的问题后添加的——这是一个好的解决方案，但它没有被纳入整体的架构图中。
- **没有预期结果定义**：每个新功能"加进去就行"，没有定义"什么算做好"的验收标准。

### 3.2 缺乏明确的系统边界

整个代码库没有清晰的分层：

```
当前状态：
  scripts/  ← 直接操作 ChromaDB 和模型，绕过核心模块
  server.py ← 直接调用所有核心模块
  pipeline.py ← 自己管理数据库、模型、文件 IO
  query_engine.py ← 自己管理数据库、模型
  config.py ← 只放了部分配置，另一部分散落各处
```

```
理想状态：
  api/          ← HTTP 层（只处理请求/响应序列化）
  core/         ← 业务逻辑层（不依赖 HTTP 框架）
    pipeline/   ← 对话处理
    retrieval/  ← 检索逻辑
    constraint/ ← 约束生成
  infrastructure/ ← 数据访问层（数据库、文件系统）
    chroma/     ← ChromaDB 封装
    storage/    ← 文件存储
```

### 3.3 没有明确的"完成"定义

在每个开发阶段结束时，没有人能回答以下问题：
- "这个功能怎样才算完成？"
- "验收标准是什么？"
- "和其他模块的集成点如何验证？"

因为没有明确的预期结果，所以每个功能都处于"基本可用"但"总有边缘情况未处理"的状态。这也解释了为什么会有 14 个"检查"和"调试"脚本——它们的存在是为了补偿核心代码缺乏可观测性。

---

## 四、结构化项目设计方法论与实践指南

### 4.1 项目生命周期模型（5 阶段）

```
┌──────────────────────────────────────────────────────────┐
│  阶段 1：需求分析                   交付物：需求规格文档    │
│  - 用户故事映射（User Story Mapping）                      │
│  - 系统上下文图（System Context Diagram）                  │
│  - 验收标准（Acceptance Criteria）                         │
├──────────────────────────────────────────────────────────┤
│  阶段 2：架构设计                   交付物：架构决策记录    │
│  - 模块分解与职责划分                                      │
│  - 接口契约定义（API 优先）                                 │
│  - 数据流设计                                              │
│  - 技术选型决策与理由（ADR）                                │
├──────────────────────────────────────────────────────────┤
│  阶段 3：迭代开发                   交付物：可运行增量      │
│  - MVP 定义（最小可行产品）                                 │
│  - 测试先行（TDD/BDD）                                     │
│  - 持续集成流水线                                          │
├──────────────────────────────────────────────────────────┤
│  阶段 4：验证与重构                 交付物：质量报告        │
│  - 集成测试                                                │
│  - 性能基准测试                                            │
│  - 代码评审                                                │
├──────────────────────────────────────────────────────────┤
│  阶段 5：交付与运维                 交付物：部署文档        │
│  - 发布说明（ Release Notes/changelog）                    │
│  - 运维手册                                                │
│  - 监控与告警                                              │
└──────────────────────────────────────────────────────────┘
```

### 4.2 架构设计框架

#### 4.2.1 分层架构模板

```
project_name/
├── src/                          # 源代码
│   ├── __init__.py
│   ├── __main__.py               # python -m project_name 入口
│   ├── api/                      # HTTP API 层
│   │   ├── __init__.py
│   │   ├── server.py             # FastAPI 应用
│   │   ├── routes/               # 路由模块
│   │   ├── middleware/           # 中间件（认证、日志、限流）
│   │   └── models/               # 请求/响应 Pydantic 模型
│   ├── core/                     # 业务逻辑层
│   │   ├── __init__.py
│   │   ├── pipeline/             # 对话处理管道
│   │   ├── retrieval/            # 检索逻辑
│   │   └── constraint/           # 约束引擎
│   ├── infrastructure/           # 基础设施层
│   │   ├── __init__.py
│   │   ├── chroma/               # ChromaDB 封装
│   │   ├── embedding/            # 嵌入服务客户端
│   │   ├── storage/              # 文件存储
│   │   └── checkpoint/           # 断点管理
│   ├── config/                   # 配置管理
│   │   ├── __init__.py
│   │   ├── settings.py           # Pydantic Settings
│   │   └── profiles/             # 多 profile 配置
│   └── common/                   # 公共工具
│       ├── __init__.py
│       ├── timer.py              # SafeTimer 唯一来源
│       └── singleton.py          # 单例工具
├── tests/                        # 测试
│   ├── conftest.py               # pytest fixtures
│   ├── unit/                     # 单元测试
│   ├── integration/              # 集成测试
│   └── performance/              # 性能测试
├── scripts/                      # 运维/管理脚本（极小）
│   └── manage.py                 # 统一 CLI 入口
├── pyproject.toml                # 项目元数据与构建配置
├── .env.example                  # 环境变量模板
└── CLAUDE.md                     # AI 辅助开发规范
```

#### 4.2.2 分层规则

| 层 | 依赖方向 | 可以做的事 | 不可以做的事 |
|---|---|---|---|
| **api/** | → core, infrastructure | 请求解析、响应序列化、调用 core | 直接操作数据库、直接处理业务逻辑 |
| **core/** | → infrastructure | 编排业务流程、调用基础设施接口 | 导入 FastAPI/uvicorn、直接处理 HTTP |
| **infrastructure/** | → 外部库 | 封装外部依赖（ChromaDB、文件系统、HTTP 客户端） | 包含业务逻辑、做决策 |
| **common/** | → 无 | 提供纯工具函数 | 导入项目其他模块 |

**关键规则：依赖方向必须单向。** Core 层不能导入 api 层的任何东西，infrastructure 层不能导入 core 层的任何东西。

### 4.3 接口契约优先（Contract-First）实践

在编写任何实现代码前，先定义接口：

```python
# 接口定义（core 层抽象）
class DialogueRepository(ABC):
    @abstractmethod
    def save_turn(self, turn: DialogueTurn) -> str: ...
    @abstractmethod
    def search(self, query: str, k: int) -> list[SearchResult]: ...

class EmbeddingService(ABC):
    @abstractmethod
    def encode(self, texts: list[str]) -> list[list[float]]: ...

# 实现（infrastructure 层）
class ChromaDialogueRepository(DialogueRepository):
    def __init__(self, client: chromadb.PersistentClient, collection_name: str):
        self._client = client
        self._collection = client.get_collection(name=collection_name)
    ...
```

这样做的好处：
1. **实现可替换**：可以轻松切换 ChromaDB → FAISS → PostgreSQL pgvector
2. **测试可 mock**：不需要真实数据库就能测试业务逻辑
3. **并行开发**：接口定义好后，API 层和 Infrastructure 层可以并行开发

### 4.4 标准化项目打包

```toml
# pyproject.toml
[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "rag-rpg"
version = "2.0.0"
description = "RAG-based memory engine for SillyTavern role-playing"
requires-python = ">=3.10"
dependencies = [
    "chromadb>=0.4.0,<0.5.0",
    "sentence-transformers>=2.2.0,<3.0.0",
    "fastapi>=0.104.0,<1.0.0",
    "uvicorn[standard]>=0.24.0,<1.0.0",
    "pydantic>=2.0.0,<3.0.0",
    "numpy>=1.24.0",
]

[project.scripts]
rag-rpg = "rag_rpg.__main__:main"
rag-rpg-manage = "rag_rpg.scripts.manage:main"

[tool.setuptools.packages.find]
where = ["src"]
```

### 4.5 测试标准

- **使用 pytest**，淘汰自建测试框架
- **测试目录结构与 src 镜像**：`tests/unit/api/`、`tests/unit/core/`、`tests/integration/`
- **测试分类**：
  - 单元测试：纯逻辑、可 mock 所有外部依赖、毫秒级执行
  - 集成测试：需要真实 ChromaDB/模型、秒级执行
  - 端到端测试：完整链路验证
- **覆盖率目标**：核心业务逻辑 > 90%，基础设施层 > 70%

### 4.6 配置管理标准化

使用 Pydantic Settings 替代散落的全局常量：

```python
# src/config/settings.py
from pydantic_settings import BaseSettings

class Settings(BaseSettings):
    # API
    api_host: str = "127.0.0.1"
    api_port: int = 8765
    api_key: str = "rag-rpg-local"

    # ChromaDB
    chroma_path: str = str(BASE_DIR / "chroma_db")
    collection_skills: str = "character_skills"
    collection_memory: str = "my_rag_memory"

    # Embedding
    model_name: str = "shibing624/text2vec-base-chinese"
    embed_timeout: float = 15.0

    # Query
    query_timeout: float = 6.0
    top_k_results: int = 3
    min_relevance: float = 0.25

    # Profile
    default_profile: str = "default"

    model_config = {"env_prefix": "RAG_RPG_", "env_file": ".env"}
```

环境变量与配置分离，不同环境（开发/测试/生产）通过 `.env` 文件管理。

---

## 五、下次项目开发前应重点关注的关键方向

### 5.1 需求分析与文档化

| 要做的事 | 具体产出 | 耗费时间估算 |
|----------|----------|-------------|
| 编写**系统上下文图**——明确项目与外部系统（SillyTavern、ChromaDB、Embedding 模型）的关系 | 1 页图表 | 0.5 天 |
| 编写**用户故事**——"作为[角色]，我想要[功能]，以便[价值]" | 10-15 个用户故事 | 1 天 |
| 定义**验收标准**——每个功能的通过/失败条件 | 每个故事 3-5 条标准 | 0.5 天 |
| 编写**数据字典**——每条数据的结构、含义、存储位置 | 1 页表格 | 0.5 天 |
| **架构决策记录（ADR）**——记录每个技术决策的理由、选项、最终选择 | 3-5 个 ADR | 0.5 天 |

**总预估算：3 天**

### 5.2 系统架构设计

| 要做的事 | 具体产出 |
|----------|----------|
| 模块划分与职责定义 | 分层架构图 + 模块接口签名 |
| 数据流设计 | 序列图：对话录入流、查询检索流、约束生成流 |
| 部署拓扑设计 | 进程模型图（主服务、嵌入服务、SillyTavern 扩展） |
| 异常处理策略 | 每个层的错误类型定义、降级策略、重试策略 |
| Profile 机制的演进方向 | 目前 Profile 支持是后加的，设计时需考虑多租户场景 |

### 5.3 开发流程规范

```
┌─────────────────────────────────────────────────────────────┐
│  每个功能的开发流程：                                        │
│                                                             │
│  1. 写需求文档（1-2 页）                                     │
│  2. 定义接口签名（Python ABC 或 Protocol）                    │
│  3. 写验收测试（pytest，mock 外部依赖）                       │
│  4. 实现功能                                                │
│  5. 运行所有测试（通过 CI）                                  │
│  6. 代码评审                                                │
│  7. 合并到主分支                                            │
└─────────────────────────────────────────────────────────────┘
```

**关键工具：**
- 静态类型检查：`mypy` 或 `pyright`（当前项目完全没有类型检查，大量 `Optional` 未标注）
- 代码格式化：`ruff format`（替代手写风格）
- 代码检查：`ruff check`（替代无标准的代码审查）
- 测试运行：`pytest`（替代自建框架）
- CI 流水线：GitHub Actions（自动运行测试、类型检查、lint）

### 5.4 风险评估与应对策略

| 风险 | 概率 | 影响 | 应对策略 |
|------|------|------|----------|
| ChromaDB 在大规模下性能下降 | 中 | 高 | 定义性能基准测试，设定 P99 延迟目标；早期评估 FAISS/PostgreSQL pgvector |
| SentenceTransformer 模型与用户中文内容不匹配 | 低 | 中 | 设计嵌入服务为可插拔接口，支持切换模型而不改业务代码 |
| 断点续执行逻辑复杂导致数据一致性问题 | 中 | 高 | 为 CheckpointManager 编写完整的状态机测试；使用数据库事务而非 JSON 文件持久化状态 |
| SillyTavern 版本升级导致扩展不兼容 | 中 | 中 | 扩展代码与 ST API 解耦，使用适配器模式 |
| 多 Profile 之间的数据隔离问题 | 中 | 高 | 在 infrastructure 层做严格的 Profile 路由测试 |

### 5.5 明确的项目预期成果定义

每个迭代的预期成果必须是**可验证**的：

| 维度 | 定义方式 | 当前项目状态 | 目标 |
|------|----------|-------------|------|
| 功能完整性 | Checklist 形式的验收标准 | 隐式、口头约定 | 明确写进 issue/story |
| 性能指标 | P50/P95/P99 延迟，吞吐量 QPS | 无定义 | 每次发布前运行性能测试 |
| 可靠性 | 无崩溃运行时长，错误率 | 无定义 | 定义 SLA（如 99.9% 请求成功率） |
| 代码质量 | 类型检查覆盖率，测试覆盖率 | 0% 类型检查，有测试但无覆盖率测量 | mypy strict 模式通过；测试覆盖 > 80% |
| 可维护性 | 圈复杂度，模块间耦合度 | 未测量 | 建立基准，每次 PR 不恶化 |
| 可观测性 | 日志、指标、追踪 | 仅有基础日志 | 新增结构化日志，关键路径加 tracing |

---

## 六、总结

rag-rpg 项目的核心问题不在于代码写得多差，而在于**没有设计就开了发**。"小脚本模式"和"问题驱动式开发"短期内交付快，但长期积累的技术债务已经显现：

- **14 个管理脚本**中至少 10 个本应是同一个 CLI 工具的子命令
- **2 份 SafeTimer 复本**和 **7 个 sys.path hack** 降低了修改代码的信心
- **无接口抽象**导致任何改动都需要同时改 3-4 个模块
- **自建测试框架**维护成本高于价值本身

好消息是：项目**功能本身是合理的**，核心逻辑（对话处理、语义检索、约束生成）的设计思路正确。当前的混乱主要是缺乏工程纪律而非思路错误。

**下一步行动建议：**
1. **不要重写**——优先建立 CI、添加类型注解、用 pytest 替换自建框架
2. **从 infrastructure 层开始重构**——先统一数据库访问和嵌入服务调用，提取公共接口
3. **逐步淘汰 sys.path hack**——通过 pyproject.toml 安装项目为可编辑包
4. **合并脚本**——将 14 个管理脚本整合为一个 `manage.py` CLI 工具

一套好的脚手架不是为了好看，而是让你在项目变复杂时仍然能自信地修改代码——而目前这个项目正处在"改一处要测三处"的阶段。

