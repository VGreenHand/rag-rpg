# RAG-RPG 项目优化进度追踪

> 每次对话开始和结束时更新此文件

---

## 总体进度

| 阶段 | 名称 | 状态 | 完成日期 |
|------|------|------|----------|
| 阶段零 | 基线建立 | ✅ 已完成 | 2026-05-14 |
| 阶段一 | 项目基础设施重构 | ✅ 已完成 | 2026-05-14 |
| 阶段二 | 公共代码抽取与去重 | ✅ 已完成 | 2026-05-14 |
| 阶段三 | 分层架构落地 | ⏳ 未开始 | — |
| 阶段四 | 脚本体系整合 | ⏳ 未开始 | — |
| 阶段五 | 测试体系重建 | ⏳ 未开始 | — |
| 阶段六 | 配置管理与环境标准化 | ⏳ 未开始 | — |
| 阶段七 | 质量门禁与持续验证 | ⏳ 未开始 | — |

---

## 过去会话记录

### 会话 1：阶段零 — 基线建立（2026-05-14）

**目标：** 创建安全网，确保后续重构可回退

**完成事项：**
- 创建 git 标签 `pre-optimization`，标记优化前基线版本
- 使用 `git clone` 创建本地完整备份 `rag-rpg-backup`
- 提交优化方案文档并推送到 GitHub（代理 127.0.0.1:7897）
- 创建并初始化 `项目优化进度.md`

**关键决策：**
- 备份方式：git tag（轻量可回溯） + git clone 双重保险
- 推送方式：`git -c http.proxy=...` 单次参数，避免修改全局配置

---

### 会话 2：pytest 学习与工具准备（2026-05-14）

**目标：** 理解 pytest 在本项目优化中的作用，确认阶段零收尾

**完成事项：**
- 安装 pytest、pytest-cov、pytest-xdist
- 理解 pytest 的 3 大用途：重构安全网、发现隐藏 Bug、质量门禁
- 确认双重备份就绪（git tag + git clone 本地备份）
- 生成本次启动提示词，供阶段一使用

**关键决策：**
- 无重大决策，本会话为知识准备和收尾

---

## 当前会话

### 会话 4：阶段二 — 公共代码抽取与去重（2026-05-14）

**目标：** 消除 SafeTimer 重复（P03）、ChromaDB 初始化重复（P05）、4 份单例样板代码

**完成事项：**
- **SafeTimer 统一（§4.1）：** 在 `src/rag_rpg/common/utils.py` 创建统一 `SafeTimer` 类，删除 `pipeline.py` 和 `query_engine.py` 中的重复定义
- **SingletonFactory 统一（§4.2）：** 在 `utils.py` 追加通用单例工厂类，将 `pipeline.py`/`query_engine.py`/`constraint_engine.py`/`checkpoint_manager.py` 末尾的 `_xxx_instances` + `get_xxx()` 替换为一行 `get_xxx = SingletonFactory(XxxClass)`
- **ChromaClient 统一（§4.3）：** 在 `src/rag_rpg/infrastructure/chroma/client.py` 创建 `ChromaClient` 封装类，修改 `pipeline.py` 和 `query_engine.py` 中 19 处 ChromaDB 调用

**修复的回归问题：**
- 原 `get_constraint_engine()` 在构造后调用了 `load_skills_from_db()`，被 `SingletonFactory` 替换后遗漏。修复方式：将 `self.load_skills_from_db()` 移入 `ConstraintEngine.__init__()`

**验证结果：**
- 所有包导入 — ✅ 成功
- 单例行为（相同 profile 同一对象，不同 profile 不同对象） — ✅ 通过
- `ConstraintEngine` 自动加载技能（3 条） — ✅ 正常
- `python -m rag_rpg` 启动 — ✅ 正常
- `/api/health` 返回正常向量库状态（8+10+15 条） — ✅ 正常
- 所有修改文件零诊断错误 — ✅

**关键决策：**
- `SafeTimer` 中使用延迟导入 `from checkpoint_manager import TimeoutError` 避免循环依赖（`utils.py` ↔ `checkpoint_manager.py`）
- `SingletonFactory` 添加 `__call__` 方法保持调用方语法不变
- `load_skills_from_db()` 移入 `__init__()` 而非给工厂加回调，改动最小化

---

## 已完成的阶段详情

### 阶段一：项目基础设施重构（2026-05-14）

**目标：** 创建标准包结构、pyproject.toml、包入口点，解决 P01/P04/P10/P11

**执行内容：**
1. 创建 `src/rag_rpg/` 目录及 5 个子包（api/core/infrastructure/config/common）
2. 创建所有子包的 `__init__.py`，使包可被 Python 发现
3. 创建 `pyproject.toml`，声明构建系统、依赖版本锁、console_scripts 入口
4. 创建 `__main__.py`，使 `python -m rag_rpg` 可启动服务
5. 执行 `pip install --no-deps -e .` 安装为可编辑包

**验证结果：**
- `python -c "import rag_rpg"` → 包导入成功
- `python -m rag_rpg` → 服务器正常启动，向量库状态正常（dialogue=8, skills=10, memory=15）
- `rag-rpg` 控制台脚本 → 可执行

**关键输出：**
- `src/rag_rpg/` 包骨架
- `pyproject.toml`（版本 2.1.0）
- 可编辑安装注册

### 阶段二：公共代码抽取与去重（2026-05-14）

**目标：** 消除 SafeTimer 重复（P03）、ChromaDB 初始化重复（P05）、4 份单例样板代码

**执行内容：**
1. **SafeTimer 统一：** 在 `src/rag_rpg/common/utils.py` 创建统一 `SafeTimer` 类，删除 `pipeline.py` 和 `query_engine.py` 中的重复定义
2. **SingletonFactory 统一：** 在 `utils.py` 追加通用单例工厂类，将 `pipeline.py`/`query_engine.py`/`constraint_engine.py`/`checkpoint_manager.py` 末尾的 `_xxx_instances` + `get_xxx()` 替换为一行 `get_xxx = SingletonFactory(XxxClass)`
3. **ChromaClient 统一：** 在 `src/rag_rpg/infrastructure/chroma/client.py` 创建 ChromaDB 封装类（延迟初始化 + 集合缓存），修改 `pipeline.py` 和 `query_engine.py` 中 19 处 ChromaDB 调用

**修复的回归问题：**
- 原 `get_constraint_engine()` 在构造后调用了 `load_skills_from_db()`，被 `SingletonFactory` 替换后遗漏。已将 `self.load_skills_from_db()` 移入 `ConstraintEngine.__init__()`

**验证结果：**
- 所有包导入 → 成功
- 单例行为（相同 profile 同一对象，不同 profile 不同对象）→ 通过
- `ConstraintEngine` 自动加载技能（3 条）→ 正常
- `python -m rag_rpg` 启动 → 正常
- `/api/health` 返回正常向量库状态（8+10+15 条）→ 正常
- 所有修改文件零诊断错误

**关键输出：**
- `src/rag_rpg/common/utils.py` — SafeTimer + SingletonFactory
- `src/rag_rpg/infrastructure/chroma/client.py` — ChromaClient
- `pipeline.py` / `query_engine.py` — 移除 `import chromadb`，统一使用 ChromaClient

### 阶段零：基线建立（2026-05-14）

**目标：** 创建安全网，确保后续重构可回退

**执行内容：**
1. 诊断项目问题并编写诊断报告（`项目设计问题诊断与方法论.md`）
2. 设计系统化优化方案（`项目优化实施方案.md`）
3. 创建进度追踪文件（`项目优化进度.md`）
4. 在 `pre-optimization` 处创建 git 基线标签
5. 提交所有优化文档到版本控制

**验证结果：**
- `git tag -l "pre-*"` → `pre-optimization` 存在
- `git status` → 工作区干净
- 可随时通过 `git checkout pre-optimization` 回退到优化前的项目状态

**关键输出：**
- 基线标签：`pre-optimization`（基于 commit `90d24b0`）
- 优化方案文档已纳入版本控制

---

## 问题追踪

| ID | 描述 | 优先级 | 状态 | 备注 |
|----|------|--------|------|------|
| P01 | 无包结构 | P0 | ✅ | 阶段一完成 |
| P02 | 模块紧耦合 | P0 | ⏳ | 阶段三处理 |
| P03 | SafeTimer 重复 | P0 | ✅ | 阶段二完成 |
| P04 | sys.path hack | P0 | 🚧 | 阶段一完成（包结构就绪，待阶段三消除剩余 hack） |
| P05 | ChromaDB 初始化重复 | P0 | ✅ | 阶段二完成 |
| P06 | 14 个脚本无组织 | P0 | ⏳ | 阶段四处理 |
| P07 | 超时参数分散 | P1 | ⏳ | 阶段六处理 |
| P08 | 脚本硬编码路径 | P1 | ⏳ | 阶段六处理 |
| P09 | 自建测试框架 | P0 | ⏳ | 阶段五处理 |
| P10 | 无 pyproject.toml | P0 | ✅ | 阶段一完成 |
| P11 | 无版本管理 | P1 | ✅ | 阶段一完成（pyproject.toml 版本 2.1.0） |
| P12 | 循环依赖风险 | P0 | ✅ | 阶段二缓解（延迟导入解耦 utils ↔ checkpoint_manager） |
| P13 | 无环境变量机制 | P1 | ⏳ | 阶段六处理 |
| P14 | 无覆盖率度量 | P1 | ⏳ | 阶段五处理 |
