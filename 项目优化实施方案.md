# RAG-RPG 项目优化实施方案

> **版本:** v1.0
> **依据:** [项目设计问题诊断与方法论](项目设计问题诊断与方法论.md)
> **目标:** 将 rag-rpg 从"小脚本集合"重构为可维护、可扩展、可交付的标准 Python 项目

---

## 目录

1. [优化总览](#一优化总览)
2. [阶段零：基线建立（准备阶段）](#二阶段零基线建立准备阶段)
3. [阶段一：项目基础设施重构](#三阶段一项目基础设施重构)
4. [阶段二：公共代码抽取与去重](#四阶段二公共代码抽取与去重)
5. [阶段三：分层架构落地](#五阶段三分层架构落地)
6. [阶段四：脚本体系整合](#六阶段四脚本体系整合)
7. [阶段五：测试体系重建](#七阶段五测试体系重建)
8. [阶段六：配置管理与环境标准化](#八阶段六配置管理与环境标准化)
9. [阶段七：质量门禁与持续验证](#九阶段七质量门禁与持续验证)
10. [资源分配与进度规划](#十资源分配与进度规划)
11. [风险评估与应对策略](#十一风险评估与应对策略)
12. [验收标准与完成定义](#十二验收标准与完成定义)

---

## 一、优化总览

### 1.1 优化目标

本方案旨在将 rag-rpg 从"功能可运行但不可维护"的状态，推进到"结构清晰、测试完备、可打包分发"的状态。具体量化目标如下：

| 维度 | 当前状态 | 优化目标 |
|------|----------|----------|
| 代码重复率 | 高（SafeTimer 2 份、sys.path 7 处、ChromaDB 初始化 5+ 处） | 零重复（公共代码 100% 抽取） |
| 架构分层 | 无分层，平铺 6 个模块 | 4 层架构（api/core/infrastructure/common） |
| 测试体系 | 自建框架，2 个文件 | pytest，30+ 测试用例，覆盖率 > 70% |
| 打包分发 | 无 pyproject.toml | 标准包结构，支持 pip install |
| 配置管理 | 硬编码与 config.py 并存 | 100% 集中配置，Pydantic Settings |
| 管理脚本 | 14 个无组织脚本 | 1 个统一 CLI 入口 |
| 导入方式 | 7 处 sys.path hack | 零 hack，标准包导入 |

### 1.2 优化阶段总览

```
┌─────────────────────────────────────────────────────────────────────┐
│ 阶段零：基线建立                         预计工时：0.5 天           │
│  - 创建完整代码备份、建立问题清单、安装测试框架                       │
├─────────────────────────────────────────────────────────────────────┤
│ 阶段一：项目基础设施重构                   预计工时：1 天            │
│  - 目录结构重组、pyproject.toml、包导入体系                          │
├─────────────────────────────────────────────────────────────────────┤
│ 阶段二：公共代码抽取与去重                 预计工时：1 天            │
│  - SafeTimer 统一、单例模式统一、ChromaDB 初始化统一                 │
├─────────────────────────────────────────────────────────────────────┤
│ 阶段三：分层架构落地                       预计工时：2.5 天          │
│  - API 层、Core 层、Infrastructure 层分离、依赖注入                  │
├─────────────────────────────────────────────────────────────────────┤
│ 阶段四：脚本体系整合                       预计工时：1 天            │
│  - CLI 工具开发、14 个脚本合并与淘汰                                 │
├─────────────────────────────────────────────────────────────────────┤
│ 阶段五：测试体系重建                       预计工时：1.5 天          │
│  - pytest 迁移、Fixture 定义、覆盖率基线                             │
├─────────────────────────────────────────────────────────────────────┤
│ 阶段六：配置管理与环境标准化               预计工时：0.5 天          │
│  - Pydantic Settings 落地、.env 分离、profile 机制统一               │
├─────────────────────────────────────────────────────────────────────┤
│ 阶段七：质量门禁与持续验证                 预计工时：1 天            │
│  - 端到端验证、性能基准、文档补全                                    │
├─────────────────────────────────────────────────────────────────────┤
│ 合计：约 9 天（按单人全职计算）                                      │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 二、阶段零：基线建立（准备阶段）

### 2.1 完整代码备份

**目的：** 确保在重构过程中任何时刻都可以回退到可用状态，降低重构风险。

**操作步骤：**

```bash
# 1. 创建完整备份（保留 git 历史）
git clone d:\Study\Project\rag-rpg d:\Study\Project\rag-rpg-backup

# 2. 或者在无 git 时直接复制
xcopy /E /I d:\Study\Project\rag-rpg d:\Study\Project\rag-rpg-backup
```

**预期效果：**
- 任何时候 `rag-rpg-backup` 目录中的代码可以直接运行，是"可回退的安全网"
- 重构过程中发现遗漏功能时，可快速参考原始实现

**实施要点：**
- 备份后验证一次 `python server.py` 能正常启动
- 备份目录放在项目目录之外，避免被重构操作误修改

---

### 2.2 建立问题清单追踪表

**目的：** 将诊断报告中的每个问题转化为可追踪的任务项，确保无一遗漏。

**操作步骤：**

在项目根目录创建问题追踪表，列出所有已知问题：

| ID | 问题类别 | 问题描述 | 涉及文件 | 优先级 | 对应优化阶段 |
|----|----------|----------|----------|--------|-------------|
| P01 | 架构 | 无 `src/` 包结构，模块平铺根目录 | 全部核心模块 | P0 | 阶段一 |
| P02 | 架构 | 模块间无接口定义，紧耦合 | pipeline.py, query_engine.py, constraint_engine.py | P0 | 阶段三 |
| P03 | 复用 | SafeTimer 重复 2 份 | pipeline.py#L47-L63, query_engine.py#L26-L43 | P0 | 阶段二 |
| P04 | 复用 | sys.path hack 出现 7 处 | 全部 scripts 文件 + tests | P0 | 阶段一 |
| P05 | 复用 | ChromaDB 初始化重复 5+ 处 | 多个文件 | P0 | 阶段二 |
| P06 | 脚本 | 14 个管理脚本无组织 | scripts/ 目录 | P0 | 阶段四 |
| P07 | 配置 | 超时参数分散在模块中 | pipeline.py#L26-L30, query_engine.py#L20-L22 | P1 | 阶段六 |
| P08 | 配置 | 脚本硬编码路径 | ingest_initial.py#L9-L12, ingest_new.py#L9-L12 | P1 | 阶段六 |
| P09 | 测试 | 自建框架 | tests/test_suite.py, tests/test_checkpoint_resume.py | P0 | 阶段五 |
| P10 | 打包 | 无 pyproject.toml | — | P0 | 阶段一 |
| P11 | 打包 | 无版本管理 | server.py 硬编码 "2.0.0" | P1 | 阶段一 |
| P12 | 耦合 | 循环依赖风险 | constraint_engine.py 内部 import chromadb | P0 | 阶段三 |
| P13 | 配置 | 无环境变量机制 | config.py | P1 | 阶段六 |
| P14 | 测试 | 无覆盖率度量 | — | P1 | 阶段五 |

**优先级定义：**
- **P0：** 必须修复，阻碍后续步骤
- **P1：** 应该修复，影响质量但非阻塞
- **P2：** 可以修复，低优先级

**预期效果：**
- 所有问题一目了然，优化进度可量化
- 每个问题都有明确的归属阶段和修复责任人

**实施要点：**
- 每完成一个优化阶段，更新问题表状态
- 发现问题时追加到表中，保持追踪完整性

---

### 2.3 安装测试基础设施

**目的：** 在重构开始前准备好测试工具，确保每一步修改都能得到验证。

**操作步骤：**

```bash
# 1. 激活项目环境
conda activate rag-rpg

# 2. 安装 pytest 及插件
pip install pytest pytest-cov pytest-xdist

# 3. 验证安装
pytest --version
pytest --cov --version

# 4. 在项目根目录创建初始 conftest.py（空的占位文件）
# 内容后续逐步填充
```

**预期效果：**
- pytest 可用，可以随时运行测试
- 为后续的"测试先行"策略做好准备

**实施要点：**
- 无需立即编写测试用例，但确保 pytest 可以正常导入项目模块
- 安装后运行 `pytest --collect-only` 验证测试发现机制正常

---

## 三、阶段一：项目基础设施重构

### 3.1 创建标准包目录结构

**目的：** 解决诊断报告中指出的"无分层结构、模块平铺根目录"问题（P01），建立符合 Python 标准工程规范的项目骨架。

**实施步骤：**

**步骤 1：创建新目录结构**

```
rag-rpg/          (项目根目录保持不变)
├── src/
│   └── rag_rpg/  (核心包，注意使用下划线而非连字符)
│       ├── __init__.py
│       ├── api/             (HTTP API 层)
│       │   ├── __init__.py
│       │   ├── server.py    (拆分自原 server.py)
│       │   ├── routes/      (路由模块)
│       │   └── models/      (Pydantic 请求/响应模型)
│       ├── core/            (业务逻辑层)
│       │   ├── __init__.py
│       │   ├── pipeline/
│       │   ├── retrieval/
│       │   └── constraint/
│       ├── infrastructure/  (基础设施层)
│       │   ├── __init__.py
│       │   ├── chroma/
│       │   ├── embedding/
│       │   ├── storage/
│       │   └── checkpoint/
│       ├── config/          (配置管理)
│       │   └── settings.py
│       └── common/          (公共工具)
│           └── utils.py
├── tests/
│   ├── conftest.py
│   ├── unit/
│   └── integration/
├── scripts/                 (保留，但内容将大幅精简)
├── data/                    (用户数据，不动)
├── pyproject.toml
└── .env.example
```

**关键原则：**
- `src/rag_rpg/` 是包名的标准写法——Python 要求包名不能含连字符，但项目名可以
- `src/` 目录是标准实践，确保测试时导入的是安装后的包而非源码，避免 import 混淆
- 每个子包都必须有 `__init__.py`

**步骤 2：编写 pyproject.toml**

```toml
[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "rag-rpg"
version = "2.1.0"
description = "RAG-based memory engine for SillyTavern role-playing"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
authors = [
    {name = "RAG-RPG Contributors"},
]

dependencies = [
    "chromadb>=0.4.0,<0.6.0",
    "sentence-transformers>=2.2.0,<3.0.0",
    "fastapi>=0.104.0,<1.0.0",
    "uvicorn[standard]>=0.24.0,<1.0.0",
    "pydantic>=2.0.0,<3.0.0",
    "pydantic-settings>=2.0.0",
    "numpy>=1.24.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "pytest-cov>=4.0.0",
    "ruff>=0.1.0",
]

[project.scripts]
rag-rpg = "rag_rpg.__main__:main"
rag-rpg-manage = "rag_rpg.scripts.manage:main"

[tool.setuptools.packages.find]
where = ["src"]
include = ["rag_rpg*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
```

**步骤 3：创建包入口点 `src/rag_rpg/__main__.py`**

```python
"""rag-rpg 包入口：python -m rag_rpg 启动主服务"""
import sys
from rag_rpg.api.server import main

if __name__ == "__main__":
    sys.exit(main())
```

**预期效果：**
- 项目可以从任何目录通过 `python -m rag_rpg` 启动
- `pip install -e .` 后可在任意位置使用 `rag-rpg` 命令
- 所有 `sys.path.insert(0, ...)` 不再需要
- 包可以在 pip 仓库发布

**实施要点：**
- 先建目录、建 `__init__.py`、建 `pyproject.toml`
- **暂不移动代码**——这一步只建空壳，代码移动由后续阶段逐一完成
- 每建一个文件就运行 `python -c "import rag_rpg"` 验证包可发现
- `pyproject.toml` 中的版本号用 `>=` 下限约束、`<=` 上限约束以防止破坏性更新

**验证标准：**
- `pip install -e .` 成功
- `python -c "from rag_rpg.config import settings; print(settings.model_dump())"` 正常输出
- `rag-rpg --help` 显示帮助信息

---

### 3.2 消除 sys.path hack

**目的：** 解决诊断报告中指出的"7 处 sys.path hack"问题（P04），这是"临时脚本"最明显的标志。

**问题背景：** 以下文件都包含 `sys.path.insert(0, str(Path(__file__).parent.parent))`:
- `scripts/check_prof.py`
- `scripts/quick_search.py`
- `scripts/setup_test_env.py`
- `scripts/validate_character_consistency.py`
- `tests/test_suite.py`
- `tests/test_checkpoint_resume.py`

这些 hack 使文件无法在安装后正常工作，必须从特定目录运行。

**实施步骤：**

**步骤 1：安装项目为可编辑包**

```bash
pip install -e d:\Study\Project\rag-rpg
```

**步骤 2：逐文件删除 sys.path hack，替换为标准导入**

以 `tests/test_suite.py` 为例：

```python
# 删除
# sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

# 替换为
from rag_rpg.config import settings
from rag_rpg.pipeline import DialoguePipeline
```

**步骤 3：验证每个文件的导入是否正常工作**

```bash
python -c "from rag_rpg.config import settings; print(settings)"
```

**预期效果：**
- 所有文件可以通过包名直接导入，不再依赖文件系统相对路径
- 文件可以被移动到任意位置而不会破坏导入
- 为后续将脚本转换为 CLI 命令做好准备

**实施要点：**
- 按依赖顺序处理：先核心模块，后脚本和测试
- 每个文件修改后立即运行 `python -c "from rag_rpg.xxx import yyy"` 验证
- 因为 `pipeline.py` 和 `query_engine.py` 等尚未移动到 `src/rag_rpg/` 下，此阶段先通过 `pip install -e .` 中 `pyproject.toml` 的 `packages.find` 配置让它们可导入

---

## 四、阶段二：公共代码抽取与去重

### 4.1 SafeTimer 统一

**目的：** 解决诊断报告中指出的"SafeTimer 完整复制了两份"问题（P03）。

**问题分析：** [pipeline.py](file:///d:/Study/Project/rag-rpg/pipeline.py#L47-L63) 和 [query_engine.py](file:///d:/Study/Project/rag-rpg/query_engine.py#L26-L43) 中的 `SafeTimer` 代码几乎完全一致，唯一的区别是被调用的上下文不同。这是一个典型的"拷贝粘贴式复用"。

**实施步骤：**

**步骤 1：在 `src/rag_rpg/common/` 下创建统一版本**

```python
# src/rag_rpg/common/utils.py
"""
公共工具模块：SafeTimer 的唯一来源
"""
import threading
import logging
from typing import Callable, TypeVar, Any

logger = logging.getLogger("rag-rpg.common")

T = TypeVar("T")


class SafeTimer:
    """带超时保护的并发执行器
    
    在独立线程中执行目标函数，超时未完成则抛出 TimeoutError。
    这是整个项目中超时保护的唯一实现来源。
    """

    @staticmethod
    def run(func: Callable[..., T], timeout: float, *args: Any, **kwargs: Any) -> T:
        result_holder: list[T] = []
        exc_holder: list[Exception] = []

        def target():
            try:
                result_holder.append(func(*args, **kwargs))
            except Exception as e:
                exc_holder.append(e)

        t = threading.Thread(target=target, daemon=True)
        t.start()
        t.join(timeout=timeout)

        if t.is_alive():
            logger.warning(f"操作超时 ({timeout}s): {func.__name__}")
            from rag_rpg.infrastructure.checkpoint import TimeoutError
            raise TimeoutError(f"操作超时 ({timeout}s)")

        if exc_holder:
            raise exc_holder[0]

        return result_holder[0] if result_holder else None
```

**步骤 2：删除原文件中的 SafeTimer 定义**

在 [pipeline.py](file:///d:/Study/Project/rag-rpg/pipeline.py) 和 [query_engine.py](file:///d:/Study/Project/rag-rpg/query_engine.py) 中删除 `SafeTimer` 类定义，改为：

```python
from rag_rpg.common.utils import SafeTimer
```

**步骤 3：全局替换所有使用点**

搜索项目中所有 `SafeTimer.run(` 的调用点，确保它们都从统一模块导入。

**预期效果：**
- 超时逻辑有唯一来源，修改行为只需改一处
- 错误修复（如日志改进、异常处理增强）一次完成、处处生效
- 代码量减少约 40 行

**实施要点：**
- 先建统一版本，再逐个文件替换——保证任何时刻至少有一个可用版本
- 替换完第一个文件后立即运行该文件的功能测试，验证超时行为一致
- 全部替换完成后，全局搜索 `class SafeTimer` 确认没有残留

---

### 4.2 单例模式统一

**目的：** 解决诊断报告中"4 个模块各自独立实现单例模式"的问题。

**问题分析：** 当前项目中存在 4 个独立的单例实现：
- [pipeline.py](file:///d:/Study/Project/rag-rpg/pipeline.py) 末尾的 `_pipeline_instances` + `get_pipeline()`
- [query_engine.py](file:///d:/Study/Project/rag-rpg/query_engine.py) 末尾的 `_query_engine_instances` + `get_query_engine()`
- [constraint_engine.py](file:///d:/Study/Project/rag-rpg/constraint_engine.py) 末尾的 `_constraint_engine_instances` + `get_constraint_engine()`
- [checkpoint_manager.py](file:///d:/Study/Project/rag-rpg/checkpoint_manager.py) 末尾的 `_checkpoint_instance` + `get_checkpoint()`

这些实现模式完全一致（字典 + 懒加载），但代码分散在 4 个文件中。

**实施步骤：**

**步骤 1：创建通用单例工厂**

```python
# src/rag_rpg/common/utils.py (追加)
class SingletonFactory:
    """通用单例工厂
    
    用法：
        pipelines = SingletonFactory(DialoguePipeline)
        engine = SingletonFactory(QueryEngine)
        
        # 获取实例
        pipe = pipelines.get(profile="default")
        qe = engine.get(profile="default")
    """
    
    def __init__(self, factory_class):
        self._factory_class = factory_class
        self._instances: dict[str, Any] = {}
    
    def get(self, *args, **kwargs) -> Any:
        key = str(args) + str(sorted(kwargs.items()))
        if key not in self._instances:
            self._instances[key] = self._factory_class(*args, **kwargs)
        return self._instances[key]
    
    def clear(self):
        self._instances.clear()
```

**步骤 2：统一替换所有单例代码**

在 `pipeline.py`、`query_engine.py`、`constraint_engine.py`、`checkpoint_manager.py` 中删除模块末尾的 `_xxx_instances` + `get_xxx()` 代码，改为：

```python
from rag_rpg.common.utils import SingletonFactory

# 在文件末尾仅保留一行
get_pipeline = SingletonFactory(DialoguePipeline)
```

**预期效果：**
- 单例模式代码量从约 20 行 × 4 处 = 80 行，缩减到 4 行
- 所有单例行为一致（包括`clear()`等测试辅助功能）
- 新增模块不用再重复写单例样板代码

**实施要点：**
- 修改后验证 `get_pipeline("default")` 重复调用返回同一个对象
- 确保 `SingletonFactory.get()` 的 key 生成策略能正确区分不同的 profile 参数

---

### 4.3 ChromaDB 客户端初始化统一

**目的：** 解决诊断报告中"5 个以上文件重复 ChromaDB 初始化序列"的问题（P05）。

**问题分析：** 以下代码在项目中反复出现：

```python
client = chromadb.PersistentClient(path=get_chroma_path(profile))
collection = client.get_or_create_collection(
    name=COLLECTION_DIALOGUE,
    metadata={"hnsw:space": "cosine"}
)
```

每次都要重复指定 `metadata={"hnsw:space": "cosine"}`，且每个模块各自维护 ChromaDB 连接。

**实施步骤：**

**步骤 1：创建统一数据访问层**

```python
# src/rag_rpg/infrastructure/chroma/client.py
"""
ChromaDB 统一客户端：项目访问向量数据库的唯一入口
"""
import logging
from typing import Optional

import chromadb

from rag_rpg.config.settings import Settings

logger = logging.getLogger("rag-rpg.infra.chroma")


class ChromaClient:
    """封装 ChromaDB PersistentClient，提供统一的数据访问接口"""
    
    def __init__(self, settings: Settings):
        self._settings = settings
        self._client: Optional[chromadb.PersistentClient] = None
        self._collections: dict[str, chromadb.Collection] = {}
    
    @property
    def client(self) -> chromadb.PersistentClient:
        if self._client is None:
            self._client = chromadb.PersistentClient(
                path=self._settings.chroma_path
            )
        return self._client
    
    def get_collection(self, name: str) -> chromadb.Collection:
        if name not in self._collections:
            self._collections[name] = self.client.get_or_create_collection(
                name=name,
                metadata={"hnsw:space": "cosine"}
            )
        return self._collections[name]
    
    def count(self, name: str) -> int:
        try:
            return self.get_collection(name).count()
        except Exception:
            return 0
```

**步骤 2：修改 pipeline.py**

```python
# 旧代码
import chromadb
from config import CHROMA_PATH, COLLECTION_DIALOGUE, COLLECTION_SKILLS
...
self.client = chromadb.PersistentClient(path=get_chroma_path(profile))
self.dialogue_col = self.client.get_or_create_collection(
    name=COLLECTION_DIALOGUE, metadata={"hnsw:space": "cosine"}
)
self.skill_col = self.client.get_or_create_collection(
    name=COLLECTION_SKILLS, metadata={"hnsw:space": "cosine"}
)

# 新代码
from rag_rpg.infrastructure.chroma import ChromaClient
...
self._chroma = ChromaClient(settings)
# 使用 self._chroma.get_collection(name) 替代 self.client 和 self.dialogue_col
```

**步骤 3：修改 query_engine.py**

类似 pipeline.py 的修改，将 `self.client` 和 `self._collections` 替换为统一的 `ChromaClient`。

**预期效果：**
- ChromaDB 连接参数（path、metadata）只存在于一个地方
- 所有模块通过同一接口访问 ChromaDB，行为一致
- 切换数据库后端（如从 ChromaDB 切换到 FAISS）时，只需修改 `ChromaClient` 一个类

**实施要点：**
- `ChromaClient` 本身不用单例模式，让调用方（如 `SingletonFactory`）决定复用策略
- `get_collection()` 内部做缓存，避免重复调用 `get_or_create_collection()` 的性能开销
- 修改后运行 `collection.count()` 验证数据可正常访问

---

## 五、阶段三：分层架构落地

### 5.1 接口契约定义（抽象层）

**目的：** 解决诊断报告中"模块间没有定义接口/抽象类，紧耦合"的问题（P02），为依赖注入奠定基础。

**实施步骤：**

**步骤 1：定义核心接口**

```python
# src/rag_rpg/core/interfaces.py
"""
核心业务接口定义：所有核心模块依赖抽象而非具体实现
"""
from abc import ABC, abstractmethod
from dataclasses import dataclass, field
from typing import Optional


@dataclass
class DialogueTurn:
    speaker: str
    name: str
    content: str
    turn: int = 0
    timestamp: Optional[str] = None
    session_id: Optional[str] = None


@dataclass
class SearchResult:
    collection: str
    id: str
    document: str
    metadata: dict
    score: float


class DialogueRepository(ABC):
    """对话数据持久化接口"""
    
    @abstractmethod
    def save_turn(self, turn: DialogueTurn, embedding: list[float]) -> str:
        ...
    
    @abstractmethod
    def search(self, query_embedding: list[float], k: int = 3) -> list[SearchResult]:
        ...
    
    @abstractmethod
    def count(self) -> int:
        ...


class EmbeddingService(ABC):
    """文本嵌入服务接口"""
    
    @abstractmethod
    def encode(self, texts: list[str]) -> list[list[float]]:
        ...


class ConstraintPolicy(ABC):
    """剧情约束策略接口"""
    
    @abstractmethod
    def generate(self, search_results: list[SearchResult], 
                 context: list[dict]) -> str:
        ...


class CheckpointStore(ABC):
    """断点持久化接口"""
    
    @abstractmethod
    def save(self, state: dict) -> None:
        ...
    
    @abstractmethod
    def load(self) -> Optional[dict]:
        ...
    
    @abstractmethod
    def clear(self) -> None:
        ...
```

**步骤 2：将现有实现适配为接口实现**

以 `DialogueRepository` 为例：

```python
# src/rag_rpg/infrastructure/chroma/repository.py
from rag_rpg.core.interfaces import DialogueRepository, SearchResult, DialogueTurn
from rag_rpg.infrastructure.chroma.client import ChromaClient


class ChromaDialogueRepository(DialogueRepository):
    """ChromaDB 实现的对话仓库"""
    
    def __init__(self, chroma_client: ChromaClient, collection_name: str):
        self._chroma = chroma_client
        self._collection_name = collection_name
    
    def save_turn(self, turn: DialogueTurn, embedding: list[float]) -> str:
        col = self._chroma.get_collection(self._collection_name)
        import uuid
        doc_id = str(uuid.uuid4())
        col.add(
            embeddings=[embedding],
            documents=[turn.content],
            metadatas=[{
                "speaker": turn.speaker,
                "name": turn.name,
                "turn": turn.turn,
                "session_id": turn.session_id or "default",
            }],
            ids=[doc_id],
        )
        return doc_id
    
    def search(self, query_embedding: list[float], k: int = 3) -> list[SearchResult]:
        col = self._chroma.get_collection(self._collection_name)
        results = col.query(
            query_embeddings=[query_embedding],
            n_results=k,
        )
        output = []
        for i in range(len(results["ids"][0])):
            output.append(SearchResult(
                collection=self._collection_name,
                id=results["ids"][0][i],
                document=results["documents"][0][i],
                metadata=results["metadatas"][0][i] if results["metadatas"] else {},
                score=round(1.0 - results["distances"][0][i], 4),
            ))
        return output
    
    def count(self) -> int:
        return self._chroma.count(self._collection_name)
```

**预期效果：**
- 核心业务逻辑（pipeline、query_engine、constraint_engine）只依赖接口，不依赖具体实现
- 测试时可以轻松 mock 数据访问层，无需真实 ChromaDB
- 未来切换数据库（如 PostgreSQL pgvector）只需新增一个实现类

**实施要点：**
- 接口定义在 `core/` 层，实现在 `infrastructure/` 层——严格遵守依赖方向
- 接口不要过度设计，只包含当前需要的操作
- 先定义最核心的 2-3 个接口，其他逐步迁移

---

### 5.2 核心模块拆分与依赖注入

**目的：** 将现有的大模块按职责拆分，通过构造函数注入依赖，消除模块间紧耦合。

**实施步骤：**

**步骤 1：拆分 pipeline.py**

当前 [pipeline.py](file:///d:/Study/Project/rag-rpg/pipeline.py) 约 300 行，承担了太多职责：文本清洗、去重、关键术语提取、熟练度检测、批量处理等。按单一职责原则拆分：

```
src/rag_rpg/core/pipeline/
├── __init__.py          # 导出 DialoguePipeline
├── pipeline.py          # 主流程编排（减至 100 行以内）
├── cleaner.py           # 文本清洗逻辑（从 pipeline 抽取）
├── dedup.py             # 去重逻辑
├── terms.py             # 关键术语提取
└── proficiency.py       # 熟练度检测
```

**步骤 2：修改 DialoguePipeline 为依赖注入模式**

```python
# src/rag_rpg/core/pipeline/pipeline.py
class DialoguePipeline:
    def __init__(
        self,
        dialogue_repo: DialogueRepository,
        skill_repo: Optional[DialogueRepository],
        embedding_service: EmbeddingService,
        cleaner: TextCleaner,
        checkpoint_store: CheckpointStore,
        settings: Settings,
    ):
        self._dialogue_repo = dialogue_repo
        self._skill_repo = skill_repo
        self._embedding = embedding_service
        self._cleaner = cleaner
        self._checkpoint = checkpoint_store
        self._settings = settings
```

**步骤 3：修改 server.py 为组合根（Composition Root）**

```python
# src/rag_rpg/api/server.py
from rag_rpg.config.settings import Settings
from rag_rpg.infrastructure.chroma import ChromaClient
from rag_rpg.infrastructure.chroma.repository import ChromaDialogueRepository
from rag_rpg.infrastructure.embedding import EmbeddingClientService
from rag_rpg.core.pipeline import DialoguePipeline, TextCleaner

def create_app(settings: Settings = None):
    if settings is None:
        settings = Settings()
    
    chroma = ChromaClient(settings)
    dialogue_repo = ChromaDialogueRepository(chroma, settings.collection_dialogue)
    skill_repo = ChromaDialogueRepository(chroma, settings.collection_skills)
    embedding = EmbeddingClientService(settings)
    cleaner = TextCleaner()
    checkpoint = ...  # CheckpointStore 实现
    
    pipeline = DialoguePipeline(
        dialogue_repo=dialogue_repo,
        skill_repo=skill_repo,
        embedding_service=embedding,
        cleaner=cleaner,
        checkpoint_store=checkpoint,
        settings=settings,
    )
    
    # 将 pipeline 注入路由
    ...
```

**步骤 4：清除 constraint_engine.py 中的内部 import**

删除 [constraint_engine.py](file:///d:/Study/Project/rag-rpg/constraint_engine.py) 中的 `import chromadb`（这是延迟导入，暗示循环依赖），改为通过构造函数注入 ChromaClient。

```python
# 旧代码
class ConstraintEngine:
    def load_skills_from_db(self):
        import chromadb  # 延迟导入
        ...

# 新代码
class ConstraintEngine:
    def __init__(self, chroma_client: ChromaClient, settings: Settings):
        self._chroma = chroma_client
        self._settings = settings
    
    def load_skills_from_db(self):
        # 直接使用 self._chroma，无需延迟导入
        ...
```

**预期效果：**
- `pipeline.py` 从 ~300 行缩减到 ~80 行（仅编排逻辑）
- 每个子模块职责清晰、可独立测试
- 依赖关系完全通过构造函数注入，`server.py` 成为唯一的组合根（Composition Root）
- 消除所有延迟导入

**实施要点：**
- 使用"绞杀者模式"（Strangler Pattern）：先在新目录中写新代码，逐步将调用方切换到新代码
- 每拆出一个子模块就运行一次集成测试，确保功能不退化
- `DialoguePipeline` 保持向后兼容的构造方式（可选参数），避免一次性修改所有调用点

---

### 5.3 跨模块数据流标准化

**目的：** 统一模块间的数据交换格式，消除"同一种数据在不同模块中用不同结构表示"的问题。

**问题分析：**
- [pipeline.py](file:///d:/Study/Project/rag-rpg/pipeline.py) 中对话用字典表示：`{"speaker": ..., "content": ...}`
- [query_engine.py](file:///d:/Study/Project/rag-rpg/query_engine.py) 中对话也用类似字典，但字段名不完全一致
- [server.py](file:///d:/Study/Project/rag-rpg/server.py) 使用 Pydantic 模型 `DialogueTurn`，但 core 层并不用这个模型

**实施步骤：**

**步骤 1：定义数据类（Data Classes）**

在 `core/interfaces.py` 中已经定义了 `DialogueTurn` 和 `SearchResult`，现在将它们作为全项目共享的数据交换格式。

**步骤 2：统一所有模块的输入输出类型**

- `DialoguePipeline.process_turn()` 接受 `DialogueTurn` 而非字典
- `QueryEngine.search()` 返回 `list[SearchResult]` 而非字典列表
- `ConstraintEngine.generate_constraints()` 接受 `list[SearchResult]` 和 `list[DialogueTurn]`

**步骤 3：在 API 层进行序列化转换**

API 层的 Pydantic 模型负责 HTTP 请求/响应的序列化，core 层的 Data Class 负责业务逻辑处理。在 API 路由中进行转换：

```python
# API 路由中
from rag_rpg.api.models import DialogueTurnRequest
from rag_rpg.core.interfaces import DialogueTurn

@app.post("/api/dialogue/ingest")
async def ingest_dialogue(req: DialogueTurnRequest, ...):
    turn = DialogueTurn(
        speaker=req.speaker,
        name=req.name,
        content=req.content,
        turn=req.turn,
        session_id=req.session_id,
    )
    result = pipeline.process_turn(turn)
    return result
```

**预期效果：**
- 数据格式在整个项目中统一，降低认知负担
- API 层和 core 层解耦：API 模型变化不影响业务逻辑
- Data Class 提供类型提示，减少运行时错误

**实施要点：**
- Core 层绝不导入 Pydantic 模型（那是 API 层的事）
- Data Class 放在 `core/interfaces.py` 中，是跨层的共享契约
- 使用 `@dataclass` 而非 Pydantic，避免 core 层依赖 Pydantic

---

## 六、阶段四：脚本体系整合

### 6.1 CLI 入口设计

**目的：** 解决诊断报告中"14 个管理脚本无组织"的问题（P06），将分散的脚本整合为统一 CLI 工具。

**问题分析：** 当前 [scripts/](file:///d:/Study/Project/rag-rpg/scripts/) 目录下有 14 个脚本，可归为以下几类：

| 类别 | 脚本 | 数量 |
|------|------|------|
| 检查类 | `check_env.py`, `check_keys.py`, `check_metadata.py`, `check_prof.py` | 4 |
| 数据导入 | `ingest_initial.py`, `ingest_new.py` | 2 |
| 数据清理 | `clean_all_data.py` | 1 |
| 数据验证 | `data_manifest.py`, `validate_character_consistency.py`, `verify_dedup_turn1.py` | 3 |
| 服务管理 | `start_services.py`, `setup_test_env.py`, `quick_search.py`, `update_skill.py` | 4 |

**实施步骤：**

**步骤 1：创建统一 CLI 入口**

```python
# src/rag_rpg/scripts/manage.py
"""
RAG-RPG 管理工具：统一 CLI 入口

用法：
    rag-rpg-manage check env          # 环境检查
    rag-rpg-manage check keys         # ChromaDB key 一致性检查
    rag-rpg-manage db clean           # 清理数据
    rag-rpg-manage db stats           # 数据统计
    rag-rpg-manage ingest initial     # 初始导入
    rag-rpg-manage ingest batch       # 批量导入新数据
    rag-rpg-manage service start      # 启动服务
"""
import argparse
import sys
from typing import NoReturn


def main() -> NoReturn:
    parser = argparse.ArgumentParser(
        description="RAG-RPG 管理工具",
        formatter_class=argparse.RawDescriptionHelpFormatter,
    )
    subparsers = parser.add_subparsers(dest="command", help="可用命令")
    
    # check 子命令
    check_parser = subparsers.add_parser("check", help="系统检查")
    check_sub = check_parser.add_subparsers(dest="check_type", help="检查类型")
    check_sub.add_parser("env", help="检查运行环境")
    check_sub.add_parser("keys", help="检查 ChromaDB key 一致性")
    check_sub.add_parser("metadata", help="查看条目元数据")
    check_sub.add_parser("prof", help="检查技能熟练度")
    
    # db 子命令
    db_parser = subparsers.add_parser("db", help="数据库操作")
    db_sub = db_parser.add_subparsers(dest="db_action", help="数据库操作类型")
    db_sub.add_parser("clean", help="清理所有数据")
    db_sub.add_parser("stats", help="数据统计")
    db_sub.add_parser("manifest", help="数据传输校验")
    
    # ingest 子命令
    ingest_parser = subparsers.add_parser("ingest", help="数据导入")
    ingest_sub = ingest_parser.add_subparsers(dest="ingest_type", help="导入类型")
    ingest_sub.add_parser("initial", help="初始导入角色数据")
    ingest_parser.add_argument("--file", "-f", help="指定 JSON 文件路径")
    
    # service 子命令
    service_parser = subparsers.add_parser("service", help="服务管理")
    service_sub = service_parser.add_subparsers(dest="service_action", help="服务操作")
    service_sub.add_parser("start", help="启动所有服务")
    service_sub.add_parser("check", help="检查服务状态")
    
    args = parser.parse_args()
    
    if args.command == "check":
        _handle_check(args)
    elif args.command == "db":
        _handle_db(args)
    elif args.command == "ingest":
        _handle_ingest(args)
    elif args.command == "service":
        _handle_service(args)
    else:
        parser.print_help()
        sys.exit(1)


def _handle_check(args):
    if args.check_type == "env":
        from rag_rpg.scripts.check_env import main as check_env
        check_env()
    elif args.check_type == "keys":
        from rag_rpg.scripts.check_keys import main as check_keys
        check_keys()
    # ... 类似处理


def _handle_db(args):
    if args.db_action == "clean":
        from rag_rpg.scripts.clean_all_data import main as clean
        clean()
    elif args.db_action == "stats":
        from rag_rpg.scripts.data_manifest import main as manifest
        manifest()
    # ...


if __name__ == "__main__":
    main()
```

**步骤 2：将原有脚本改造为标准模块**

以 `check_env.py` 为例：

```python
# scripts/check_env.py → 改造后成为可导入模块
def main():
    """环境检查的主逻辑"""
    import sys
    from rag_rpg.config.settings import Settings
    
    settings = Settings()
    print(f"Python: {sys.version}")
    print(f"ChromaDB 路径: {settings.chroma_path}")
    print(f"嵌入模型: {settings.model_name}")
    # ...

if __name__ == "__main__":
    main()  # 仍然可以直接运行
```

**步骤 3：合并功能重叠的 check_* 脚本**

4 个 `check_*` 脚本可以合并为一个 `check` 模块：

```
src/rag_rpg/scripts/check/
├── __init__.py
├── env.py         # 原 check_env.py
├── keys.py        # 原 check_keys.py
├── metadata.py    # 原 check_metadata.py
└── prof.py        # 原 check_prof.py
```

**步骤 4：处理需要保留直接运行的脚本**

像 `clean_all_data.py` 这类有破坏性的操作，保留脚本但要求确认：

```python
def main():
    """清理所有 chroma_db 数据"""
    import shutil
    from pathlib import Path
    
    confirm = input("⚠️ 此操作将删除所有向量数据！确认请输 YES: ")
    if confirm != "YES":
        print("已取消")
        return
    
    chroma_dir = Path("chroma_db")
    if chroma_dir.exists():
        shutil.rmtree(chroma_dir)
        print("✅ 已清理所有数据")
    else:
        print("无数据可清理")
```

**预期效果：**
- 14 个混乱的脚本 → 1 个 CLI 工具 + 少数保留的模块
- 用户只需记住 `rag-rpg-manage` 一个命令
- 脚本功能复用项目基础设施，不再重复加载模型和数据库

**实施要点：**
- 保持向后兼容：原有脚本的 `if __name__ == "__main__"` 入口保留，不破坏现有工作流
- 使用 argparse 而非 click/typer，减少外部依赖
- 先实现最常用的 3-5 个命令，逐步覆盖全部功能
- 删除脚本前确认没有人/文档还在引用它

---

## 七、阶段五：测试体系重建

### 7.1 pytest 迁移

**目的：** 解决诊断报告中"自建框架而非标准工具"的问题（P09），将测试从自定义框架迁移到 pytest。

**实施步骤：**

**步骤 1：创建 pytest 配置**

```python
# tests/conftest.py
"""
pytest 共享 Fixtures：所有测试模块共享的测试基础设施
"""
import pytest
from pathlib import Path
import tempfile
import shutil

from rag_rpg.config.settings import Settings


@pytest.fixture
def temp_dir():
    """每个测试用例独立的临时目录"""
    tmp = Path(tempfile.mkdtemp())
    yield tmp
    shutil.rmtree(tmp, ignore_errors=True)


@pytest.fixture
def test_settings(temp_dir) -> Settings:
    """使用临时目录的测试配置"""
    settings = Settings()
    # 使用内存模式或临时目录，避免影响开发数据
    settings.chroma_path = str(temp_dir / "chroma_db")
    return settings


@pytest.fixture
def mock_embedding_service(mocker):
    """Mock 嵌入服务，避免加载真实模型"""
    mock = mocker.MagicMock()
    mock.encode.return_value = [[0.1] * 768]
    return mock


@pytest.fixture
def sample_dialogue_turn():
    """标准测试用对话数据"""
    from rag_rpg.core.interfaces import DialogueTurn
    return DialogueTurn(
        speaker="user",
        name="玩家",
        content="我拔出长剑，冲向敌人",
        turn=1,
        session_id="test-session-001",
    )
```

**步骤 2：测试目录结构**

```
tests/
├── conftest.py                 # 共享 fixtures
├── __init__.py
├── unit/
│   ├── __init__.py
│   ├── test_cleaner.py         # 文本清洗测试
│   ├── test_dedup.py           # 去重逻辑测试
│   ├── test_query.py           # 查询策略测试
│   ├── test_constraint.py      # 约束生成测试
│   └── test_checkpoint.py      # 断点管理测试
├── integration/
│   ├── __init__.py
│   ├── test_pipeline.py        # pipeline 集成测试
│   ├── test_query_engine.py    # 查询引擎集成测试
│   └── test_api.py             # API 端点测试
└── performance/
    ├── __init__.py
    └── test_benchmark.py       # 性能基准测试
```

**步骤 3：编写第一个单元测试——SafeTimer**

```python
# tests/unit/test_safe_timer.py
import time
import pytest
from rag_rpg.common.utils import SafeTimer
from rag_rpg.infrastructure.checkpoint import TimeoutError


class TestSafeTimer:
    def test_normal_execution(self):
        result = SafeTimer.run(lambda: 42, timeout=5.0)
        assert result == 42
    
    def test_timeout(self):
        def slow_func():
            time.sleep(10)
            return 42
        
        with pytest.raises(TimeoutError):
            SafeTimer.run(slow_func, timeout=0.5)
    
    def test_exception_propagation(self):
        def failing_func():
            raise ValueError("预期错误")
        
        with pytest.raises(ValueError, match="预期错误"):
            SafeTimer.run(failing_func, timeout=5.0)
    
    def test_with_args(self):
        result = SafeTimer.run(lambda a, b: a + b, timeout=5.0, 1, 2)
        assert result == 3
```

**步骤 4：迁移原 test_suite.py 中的测试用例**

[test_suite.py](file:///d:/Study/Project/rag-rpg/tests/test_suite.py) 是一个 700+ 行的单文件，覆盖了功能测试、兼容性测试和性能测试。采用以下策略拆分：

1. **功能测试** → `tests/unit/`（可 mock 外部依赖，毫秒级执行）
2. **集成测试** → `tests/integration/`（需要真实 ChromaDB，秒级执行）
3. **性能测试** → `tests/performance/`（需要基准环境，分钟级执行）

以"文本清洗"测试为例，将原 `test_suite.py` 中的测试用例改写为 pytest 风格：

```python
# tests/unit/test_cleaner.py
import pytest
from rag_rpg.core.pipeline.cleaner import TextCleaner


class TestTextCleaner:
    @pytest.fixture
    def cleaner(self):
        return TextCleaner()
    
    def test_remove_html_tags(self, cleaner):
        assert cleaner.clean("<p>你好</p>") == "你好"
    
    def test_remove_markdown_emphasis(self, cleaner):
        assert cleaner.clean("**重要** *斜体*") == "重要 斜体"
    
    def test_collapse_newlines(self, cleaner):
        assert cleaner.clean("行1\n\n\n行2") == "行1\n\n行2"
    
    def test_collapse_spaces(self, cleaner):
        assert cleaner.clean("这是    很多    空格") == "这是 很多 空格"
    
    def test_empty_input(self, cleaner):
        assert cleaner.clean("") == ""
        assert cleaner.clean(None) == ""
        assert cleaner.clean("   ") == ""
```

**预期效果：**
- 测试用例从 2 个文件 → 10+ 个文件，每个测试文件职责单一
- 测试用例数从 ~50 个（粗略估算）→ 80+ 个（覆盖更多边缘情况）
- 测试运行时间大幅缩短（单元测试 < 5 秒）
- 获得覆盖率报告（`pytest --cov`）

**实施要点：**
- 不要一次性重写所有测试。先迁移核心功能的测试用例，运行通过后再迁移其他
- 使用 `pytest.mark` 标记需要真实数据库的测试：`@pytest.mark.integration`
- 在 conftest.py 中提供 mock embedding 和 mock chroma 的 fixture
- 运行测试时：`pytest tests/unit/`（快速）、`pytest tests/`（完整）

---

### 7.2 覆盖率与边界测试

**目的：** 确保核心模块的代码覆盖率达标，并覆盖关键边界条件。

**实施步骤：**

**步骤 1：定义覆盖率目标**

| 模块 | 目标覆盖率 | 说明 |
|------|-----------|------|
| `common/` | 95%+ | 工具函数，逻辑简单 |
| `core/pipeline/cleaner.py` | 95%+ | 字符串处理，易于覆盖 |
| `core/pipeline/dedup.py` | 90%+ | 去重逻辑，有边界条件 |
| `core/retrieval/` | 85%+ | 查询策略，部分逻辑依赖外部 |
| `core/constraint/` | 85%+ | 约束生成，分支较多 |
| `infrastructure/chroma/` | 75%+ | 数据库操作，部分需集成测试 |

**步骤 2：针对诊断报告中的问题编写边界测试**

例如，针对"去重阈值边界"问题：

```python
# tests/unit/test_dedup.py
import pytest
import numpy as np


class TestDedupThreshold:
    """去重阈值边界测试——诊断报告中未覆盖的边界情况"""
    
    def test_threshold_exact_boundary(self):
        """刚好在阈值上的情况"""
        from rag_rpg.core.pipeline.dedup import is_duplicate
        # 相似度 = 0.92（刚好等于阈值）
        assert is_duplicate(np.array([0.08]), threshold=0.92) is True
    
    def test_threshold_just_below(self):
        """略低于阈值的情况"""
        from rag_rpg.core.pipeline.dedup import is_duplicate
        # 相似度 = 0.9199（略低于阈值）
        assert is_duplicate(np.array([0.0801]), threshold=0.92) is False
    
    def test_threshold_zero(self):
        """阈值为 0 的极端情况"""
        from rag_rpg.core.pipeline.dedup import is_duplicate
        assert is_duplicate(np.array([0.5]), threshold=0.0) is True  # 任何结果都算重复
    
    def test_threshold_one(self):
        """阈值为 1 的极端情况"""
        from rag_rpg.core.pipeline.dedup import is_duplicate
        assert is_duplicate(np.array([0.0]), threshold=1.0) is False  # 只有完全一致才算重复
    
    def test_empty_embedding(self):
        """空向量的边缘情况"""
        from rag_rpg.core.pipeline.dedup import is_duplicate
        with pytest.raises(ValueError):
            is_duplicate(np.array([]), threshold=0.92)
```

**预期效果：**
- 核心模块覆盖率 ≥ 85%
- 所有诊断报告中提到的"边缘情况未处理"问题有对应的测试用例
- 新增代码必须带有测试用例（通过 CI 门禁强制执行）

**实施要点：**
- 先测量当前覆盖率：`pytest --cov=rag_rpg --cov-report=term-missing`
- 针对覆盖报告中标记为红色的行编写测试
- 边界测试优先于常规功能测试

---

## 八、阶段六：配置管理与环境标准化

### 8.1 Pydantic Settings 落地

**目的：** 解决诊断报告中"配置散落：硬编码与配置文件并存"的问题（P07, P08, P13），将所有配置集中管理。

**实施步骤：**

**步骤 1：用 Pydantic Settings 重写 config.py**

```python
# src/rag_rpg/config/settings.py
"""
集中配置管理：所有项目配置的唯一来源
通过环境变量 + .env 文件覆盖，支持多 profile
"""
from pathlib import Path
from typing import Optional
from pydantic_settings import BaseSettings, SettingsConfigDict

BASE_DIR = Path(__file__).resolve().parent.parent.parent.parent  # src/../.. → 项目根


class Settings(BaseSettings):
    # ─── API 服务 ────────────────────────────────────
    api_host: str = "127.0.0.1"
    api_port: int = 8765
    api_key: str = "rag-rpg-local"

    # ─── ChromaDB ────────────────────────────────────
    chroma_path: str = str(BASE_DIR / "chroma_db")
    collection_skills: str = "character_skills"
    collection_memory: str = "my_rag_memory"
    collection_dialogue: str = "dialogue_memory"
    collection_plot_state: str = "plot_state"

    # ─── 嵌入模型 ────────────────────────────────────
    model_name: str = "shibing624/text2vec-base-chinese"
    embedding_service_host: str = "127.0.0.1"
    embedding_service_port: int = 8766

    # ─── 超时控制（从各模块集中到此） ─────────────────
    embed_timeout: float = 15.0      # 原 pipeline.py
    chroma_timeout: float = 8.0      # 原 pipeline.py
    file_io_timeout: float = 5.0     # 原 pipeline.py
    query_timeout: float = 6.0       # 原 query_engine.py
    encode_timeout: float = 8.0      # 原 query_engine.py
    collection_get_timeout: float = 4.0  # 原 query_engine.py

    # ─── 查询参数 ────────────────────────────────────
    max_context_turns: int = 5
    top_k_results: int = 3
    min_relevance: float = 0.25
    max_constraint_chars: int = 800

    # ─── 去重 ────────────────────────────────────────
    dedup_similarity_threshold: float = 0.92

    # ─── 约束引擎 ────────────────────────────────────
    constraint_cooldown_turns: int = 3
    max_active_constraints: int = 5

    # ─── Profile ─────────────────────────────────────
    default_profile: str = "default"

    # ─── 路径 ────────────────────────────────────────
    dialogue_dir: str = str(BASE_DIR / "dialogues")
    batch_file: str = str(BASE_DIR / "new_batch.txt")

    model_config = SettingsConfigDict(
        env_prefix="RAG_RPG_",
        env_file=".env",
        env_file_encoding="utf-8",
        extra="ignore",
    )

    def get_chroma_path(self, profile: Optional[str] = None) -> str:
        if profile is None or profile == self.default_profile:
            return self.chroma_path
        return str(Path(self.chroma_path) / profile)

    def get_dialogue_dir(self, profile: Optional[str] = None) -> Path:
        base = Path(self.dialogue_dir)
        if profile is None or profile == self.default_profile:
            return base
        return base / profile

    def get_checkpoint_dir(self, profile: Optional[str] = None) -> Path:
        base = BASE_DIR / ".checkpoints"
        if profile is None or profile == self.default_profile:
            return base
        return base / profile


# 全局默认配置实例
settings = Settings()
```

**步骤 2：创建 .env.example**

```env
# RAG-RPG 环境变量配置
# 复制此文件为 .env 并按需修改
# 所有配置前缀为 RAG_RPG_

# API 服务
RAG_RPG_API_HOST=127.0.0.1
RAG_RPG_API_PORT=8765
RAG_RPG_API_KEY=rag-rpg-local

# ChromaDB 路径
RAG_RPG_CHROMA_PATH=./chroma_db

# 嵌入服务
RAG_RPG_MODEL_NAME=shibing624/text2vec-base-chinese
RAG_RPG_EMBEDDING_SERVICE_HOST=127.0.0.1
RAG_RPG_EMBEDDING_SERVICE_PORT=8766

# 性能参数
RAG_RPG_QUERY_TIMEOUT=6.0
RAG_RPG_TOP_K_RESULTS=3
RAG_RPG_MIN_RELEVANCE=0.25
```

**步骤 3：从各模块中删除散落的常量**

逐一删除以下文件中的模块级常量：

- [pipeline.py](file:///d:/Study/Project/rag-rpg/pipeline.py#L26-L30)：删除 `EMBED_TIMEOUT`、`CHROMA_TIMEOUT`、`FILE_IO_TIMEOUT` 等
- [query_engine.py](file:///d:/Study/Project/rag-rpg/query_engine.py#L20-L22)：删除 `QUERY_TIMEOUT`、`ENCODE_TIMEOUT`、`COLLECTION_GET_TIMEOUT` 等
- 所有脚本中的 `JSON_FILE`、`COLLECTION_NAME`、`MODEL_NAME` 硬编码

替换为：

```python
from rag_rpg.config.settings import settings
# 使用 settings.embed_timeout、settings.query_timeout 等
```

**预期效果：**
- 所有配置集中在一个文件中，一目了然
- 环境变量可以覆盖任何配置，支持不同环境的差异化配置
- 超时参数不再散落各处，修改超时只需改一处
- 脚本不再硬编码文件路径和模型名称

**实施要点：**
- `Settings` 中使用 `model_config` 的 `extra="ignore"` 选项，避免 .env 文件中存在额外变量时报错
- `get_chroma_path()` 等方法放在 Settings 类中，保持路径计算逻辑集中
- 先完成配置集中，再逐模块替换引用——保证任何时刻项目可运行

---

### 8.2 多环境配置策略

**目的：** 支持开发、测试、生产三种环境的配置隔离。

**实施步骤：**

**步骤 1：创建环境文件模板**

```
.env.development    # 开发环境：本地 ChromaDB，debug 日志
.env.test           # 测试环境：临时目录，mock 模型
.env.production     # 生产环境：生产配置（暂无）
```

**步骤 2：通过环境变量选择环境**

```python
# src/rag_rpg/config/settings.py
import os

ENV = os.environ.get("RAG_RPG_ENV", "development")
env_file = f".env.{ENV}" if os.path.exists(f".env.{ENV}") else ".env"

class Settings(BaseSettings):
    ...
    model_config = SettingsConfigDict(
        env_prefix="RAG_RPG_",
        env_file=env_file,
        extra="ignore",
    )
```

**步骤 3：为测试环境创建专用配置**

```python
# tests/conftest.py 中使用临时目录覆盖配置
@pytest.fixture
def test_settings(temp_dir) -> Settings:
    os.environ["RAG_RPG_CHROMA_PATH"] = str(temp_dir / "chroma_db")
    os.environ["RAG_RPG_DIALOGUE_DIR"] = str(temp_dir / "dialogues")
    os.environ["RAG_RPG_API_KEY"] = "test-key"
    # 重新加载配置
    from rag_rpg.config.settings import Settings
    return Settings()
```

**预期效果：**
- 开发环境和测试环境完全隔离，测试不会污染开发数据
- 切换环境只需设置一个环境变量
- 测试可以并行运行而不相互干扰

**实施要点：**
- `.env` 文件不提交到版本控制，`.env.example` 提交
- 测试 fixture 中使用 `monkeypatch` 或 `os.environ` 覆盖配置，确保隔离

---

## 九、阶段七：质量门禁与持续验证

### 9.1 端到端验证

**目的：** 确保所有模块重构后，整体功能与重构前一致。

**实施步骤：**

**步骤 1：创建端到端测试**

```python
# tests/integration/test_e2e.py
"""
端到端验证：模拟完整的"对话输入 → 存储 → 检索 → 约束生成"链路
"""
import pytest
from pathlib import Path

from rag_rpg.config.settings import Settings
from rag_rpg.core.interfaces import DialogueTurn


@pytest.mark.e2e
class TestEndToEnd:
    """端到端链路测试（需要真实 ChromaDB）"""
    
    def test_ingest_then_query(self, test_settings):
        """测试：写入一条对话 → 查询能否检索到"""
        # 1. 创建 pipeline
        from rag_rpg.infrastructure.chroma import ChromaClient
        from rag_rpg.infrastructure.chroma.repository import ChromaDialogueRepository
        from rag_rpg.core.pipeline.pipeline import DialoguePipeline
        
        chroma = ChromaClient(test_settings)
        repo = ChromaDialogueRepository(chroma, test_settings.collection_dialogue)
        pipeline = DialoguePipeline(dialogue_repo=repo, ...)
        
        # 2. 处理一条对话
        turn = DialogueTurn(
            speaker="user", name="玩家",
            content="我使用火焰魔法攻击敌人",
            turn=1, session_id="e2e-test",
        )
        result = pipeline.process_turn(turn)
        assert result["key_terms_found"] is not None
        
        # 3. 查询相关内容
        from rag_rpg.core.retrieval.query import QueryEngine
        engine = QueryEngine(...)
        search_results = engine.multi_search(
            dialogue_context=[{"speaker": "user", "content": "火焰魔法"}],
        )
        assert len(search_results["results"]) > 0
    
    def test_checkpoint_resume(self, test_settings):
        """测试：批量导入中断后断点恢复"""
        # 创建测试数据文件
        batch_file = Path(test_settings.batch_file)
        batch_file.write_text(
            "[SKILL] 火焰魔法\n[SKILL] 冰霜护盾\n[MEMORY] 打败了恶龙\n",
            encoding="utf-8"
        )
        # 启动批量导入，中断，恢复，验证不重复
        ...
```

**步骤 2：功能对比测试**

将原始代码的输出与新重构代码的输出进行对比：

```python
@pytest.mark.comparison
def test_output_consistency():
    """验证新代码的输出格式与旧代码一致"""
    old_output = run_old_pipeline("测试文本")
    new_output = run_new_pipeline("测试文本")
    
    assert old_output["key_terms"] == new_output["key_terms"]
    assert old_output["cleaned_text"] == new_output["cleaned_text"]
```

**预期效果：**
- 核心功能链路（ingest → search → constrain）在所有阶段重构后正常运行
- 可以通过一个命令执行完整验证：`pytest tests/integration/test_e2e.py`

**实施要点：**
- 端到端测试标记为 `@pytest.mark.e2e`，默认不运行（耗时较长）
- 在 CI 中单独配置 e2e 测试的运行条件
- 保留原始代码的输出样本（snapshot testing），作为回归基准

---

### 9.2 最终检查清单

**目的：** 在宣布优化完成前，逐项确认所有目标已达成。

```python
# tools/verify_optimization.py
"""
优化完成度检查脚本

运行方式：
    python tools/verify_optimization.py

输出：
    ✅ 或 ❌ 标记每个检查项的状态
"""

CHECKS = [
    # 阶段一：基础设施
    ("P01", "包结构", "rag_rpg 包可导入", 
     lambda: __import__("rag_rpg")),
    ("P01", "包入口", "python -m rag_rpg 可运行",
     lambda: __import__("rag_rpg.__main__")),
    ("P10", "pyproject.toml", "项目元数据完整",
     lambda: Path("pyproject.toml").exists()),
    
    # 阶段二：代码去重
    ("P03", "SafeTimer 唯一", "全局只在一个文件定义 SafeTimer",
     lambda: count_occurrences("class SafeTimer") == 1),
    ("P05", "Chroma 初始化唯一", "ChromaClient 唯一初始化入口",
     lambda: count_occurrences("PersistentClient") == 1),
    
    # 阶段三：分层架构
    ("P02", "接口定义", "core/interfaces.py 存在",
     lambda: Path("src/rag_rpg/core/interfaces.py").exists()),
    ("P12", "无延迟导入", "constraint_engine 不再内部 import chromadb",
     lambda: "import chromadb" not in Path("src/rag_rpg/core/constraint/engine.py").read_text()),
    
    # 阶段四：脚本整合
    ("P06", "CLI 工具", "rag-rpg-manage 可运行",
     lambda: subprocess.run(["rag-rpg-manage", "--help"], capture_output=True).returncode == 0),
    
    # 阶段五：测试体系
    ("P09", "pytest", "测试可以通过 pytest 运行",
     lambda: subprocess.run(["pytest", "tests/unit/", "--collect-only"], capture_output=True).returncode == 0),
    ("P14", "覆盖率", "核心模块覆盖率 >= 80%",
     lambda: check_coverage()),
    
    # 阶段六：配置管理
    ("P07", "配置集中", "所有超时参数在 Settings 中",
     lambda: check_all_timeouts_in_settings()),
    ("P08", "无硬编码", "脚本中无硬编码路径",
     lambda: check_no_hardcoded_paths()),
]
```

**预期效果：**
- 一个命令即可全面验证优化进度
- 所有诊断报告中的问题都有明确的验证项
- 新增团队成员可以通过检查清单快速了解项目状态

**实施要点：**
- 每个检查项必须可以自动化验证
- 检查脚本纳入 CI 流程，每次提交自动运行
- 检查项随项目演进更新

---

## 十、资源分配与进度规划

### 10.1 阶段依赖关系

```
阶段零 ──→ 阶段一 ──→ 阶段二 ──→ 阶段三 ──→ 阶段四
   │                                              │
   └──────────── 阶段六 ───────────────            │
                              ↓                   ↓
                         阶段五 ←─── 阶段七 ────
```

- **阶段零（基线建立）** 是所有阶段的前提
- **阶段一（基础设施）** 必须在阶段二、三、四之前
- **阶段二（去重）** 最好在阶段三（分层）之前，也可以并行
- **阶段三（分层）** 是核心工作，依赖阶段一和阶段二
- **阶段四（脚本整合）** 依赖阶段三
- **阶段五（测试）** 可以在阶段三开始后逐步推进，无需等待完成
- **阶段六（配置）** 独立，可以随时进行
- **阶段七（验证）** 是最后阶段

### 10.2 建议排期（单人全职）

| 周次 | 周一 | 周二 | 周三 | 周四 | 周五 |
|------|------|------|------|------|------|
| **Week 1** | 阶段零 | 阶段一 | 阶段一+二 | 阶段二 | 阶段三 |
| **Week 2** | 阶段三 | 阶段三 | 阶段三+四 | 阶段四+五 | 阶段五+六 |
| **Week 3** | 阶段五+六 | 阶段七 | 阶段七+缓冲 | 缓冲/修复 | 交付 |

### 10.3 多人协作分工（如有人力）

| 角色 | 职责 | 负责阶段 |
|------|------|----------|
| 架构师 | 接口设计、代码审查 | 阶段一、三 |
| 开发者 A | 基础设施、配置 | 阶段一、六 |
| 开发者 B | 代码去重、脚本整合 | 阶段二、四 |
| 开发者 C | 测试体系 | 阶段五 |
| QA | 端到端验证 | 阶段七 |

### 10.4 排期原则

- **每个阶段都包含验证时间**：实施时间 = 纯编码时间 × 1.5
- **阶段三（分层架构）分配最多时间**：这是最复杂、风险最高的阶段
- **预留 20% 缓冲时间**：在项目排期最后留出 1-2 天处理意外问题
- **每日集成**：即使代码未完成，也至少每天运行一次 pytest，确保不引入回归错误

---

## 十一、风险评估与应对策略

### 11.1 风险矩阵

| 风险 ID | 风险描述 | 概率 | 影响 | 等级 | 应对策略 |
|---------|----------|------|------|------|----------|
| R01 | 重构破坏现有功能，影响已部署的 SillyTavern 实例 | 中 | 高 | **高** | 阶段零完整备份 + 每个阶段运行 e2e 测试 + 兼容旧 API |
| R02 | 将 SafeTimer 抽取为公共模块时，因未发现的线程安全问题导致死锁 | 低 | 高 | 中 | 保留旧实现作为后备（fallback），新实现在测试环境中充分验证 |
| R03 | 依赖注入改造引入循环依赖 | 中 | 中 | 中 | 严格遵循单向依赖规则（api → core → infrastructure） |
| R04 | pytest 迁移过程中遗漏原有测试覆盖的功能 | 中 | 中 | 中 | 先以"运行通过"为目标，再补充覆盖率报告进行比较 |
| R05 | 配置集中化后，忘记了某个硬编码常量的引用点 | 低 | 低 | 低 | 在阶段七中使用脚本全局搜索硬编码模式 |
| R06 | 脚本整合时破坏了用户的自动化工作流 | 中 | 中 | 中 | 保留旧脚本入口（deprecation warning），给出迁移期 |

### 11.2 回滚策略

如果某个阶段的修改导致严重问题：

```
1. 停止当前阶段工作
2. 恢复到阶段零创建的备份
3. 记录失败原因，调整方案
4. 在隔离分支上重试
```

### 11.3 渐进式交付策略

不建议一次性完成所有优化然后发布。推荐采用**渐进式交付**：

1. **里程碑 1（Week 1 结束）**：项目可安装为包，SafeTimer 统一，配置集中
   - 交付物：`pip install -e .` 可工作，`pytest tests/unit/` 全部通过
2. **里程碑 2（Week 2 中）**：分层架构完成，pytest 测试覆盖核心功能
   - 交付物：核心模块迁移到 `src/rag_rpg/` 下，旧模块保留兼容性入口
3. **里程碑 3（Week 3 结束）**：所有优化完成
   - 交付物：新架构全面运行，旧代码完全退役

---

## 十二、验收标准与完成定义

### 12.1 总体验收标准

| 编号 | 验收标准 | 验证方式 |
|------|----------|----------|
| AC01 | 项目可以 `pip install -e .` 安装 | 运行安装命令 |
| AC02 | 项目可以通过 `rag-rpg` 命令启动 | 启动并在浏览器访问 `/api/health` |
| AC03 | 所有测试通过（单元 + 集成） | `pytest tests/` 零失败 |
| AC04 | 核心模块代码覆盖率 ≥ 80% | `pytest --cov=rag_rpg.core` |
| AC05 | 0 处 `sys.path.insert` hack | 全局搜索确认 |
| AC06 | 0 次 `class SafeTimer` 重复 | 全局搜索确认 |
| AC07 | 所有配置集中在 Settings 类 | 检查 config/settings.py |
| AC08 | 14 个脚本整合为 ≤ 5 个 CLI 命令 | 查看 src/rag_rpg/scripts/manage.py |
| AC09 | 所有测试使用 pytest | `pytest --collect-only` 无自定义框架 |
| AC10 | 模块依赖方向单向（api → core → infra） | 代码审查 |

### 12.2 "完成"的定义（Definition of Done）

每个阶段/任务完成前必须满足：

- [ ] 代码已合并到主分支
- [ ] 相关测试已编写并通过
- [ ] 无 lint 错误（ruff 检查通过）
- [ ] 旧代码已清理（无重复、无废弃导入）
- [ ] 不影响核心功能（端到端测试通过）
- [ ] 配置项已更新到集中配置或 .env.example

---

## 附录 A：关联文件索引

| 诊断问题 | 涉及文件 | 优化阶段 |
|----------|----------|----------|
| 无包结构 | 全部核心模块 | 阶段一 |
| SafeTimer 重复 | [pipeline.py](file:///d:/Study/Project/rag-rpg/pipeline.py#L47-L63), [query_engine.py](file:///d:/Study/Project/rag-rpg/query_engine.py#L26-L43) | 阶段二 |
| sys.path hack | [7 个文件](file:///d:/Study/Project/rag-rpg/scripts/) | 阶段一 |
| ChromaDB 初始化重复 | 5+ 个文件 | 阶段二 |
| 配置散落 | [pipeline.py](file:///d:/Study/Project/rag-rpg/pipeline.py#L26-L30), [query_engine.py](file:///d:/Study/Project/rag-rpg/query_engine.py#L20-L22), [ingest_*.py](file:///d:/Study/Project/rag-rpg/scripts/) | 阶段六 |
| 自建测试框架 | [tests/test_suite.py](file:///d:/Study/Project/rag-rpg/tests/test_suite.py), [tests/test_checkpoint_resume.py](file:///d:/Study/Project/rag-rpg/tests/test_checkpoint_resume.py) | 阶段五 |
| 无 pyproject.toml | — | 阶段一 |
| 脚本无组织 | [scripts/ 目录](file:///d:/Study/Project/rag-rpg/scripts/) (14 个文件) | 阶段四 |
| 延迟导入 | [constraint_engine.py](file:///d:/Study/Project/rag-rpg/constraint_engine.py) | 阶段三 |

---

## 附录 B：快速参考命令

```bash
# ─── 开发环境 ──────────────────────────────────
# 安装项目为可编辑包
pip install -e d:\Study\Project\rag-rpg

# 运行所有测试
pytest tests/

# 运行单元测试（快速）
pytest tests/unit/ -v

# 运行集成测试（慢）
pytest tests/integration/ -v

# 带覆盖率报告
pytest --cov=rag_rpg --cov-report=term-missing tests/

# ─── 管理命令 ──────────────────────────────────
# 启动服务
rag-rpg  # 或 python -m rag_rpg

# 环境检查
rag-rpg-manage check env

# 数据统计
rag-rpg-manage db stats

# 清理数据
rag-rpg-manage db clean

# ─── 优化验证 ──────────────────────────────────
# 运行优化完成度检查
python tools/verify_optimization.py
```

---

> **附注：** 本方案基于 [项目设计问题诊断与方法论](项目设计问题诊断与方法论.md) 中识别的问题设计，所有阶段均以"可运行、可验证、可回滚"为原则。实施过程中如发现方案与实际不符，应及时调整而非强行遵循。
