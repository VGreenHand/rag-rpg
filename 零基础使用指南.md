# RAG-RPG 记忆引擎 · 零基础使用指南

> 写给第一次接触 RAG-RPG 的你。

---

## 一、先搞懂几个概念（不搞懂也能用，但懂了更好）

### 1.1 大模型有个"健忘症"

用过 AI 聊天的朋友可能遇到过这种情况：

- 你告诉 AI "我的角色叫无名，使用光剑"
- AI 记住了，回复得很精彩
- 聊了 20 轮之后，AI 突然问："你的武器是什么？"
- 你：？？？刚才不是说了吗？？？

这就是大模型的**健忘症**——它每次回复只看到最近几轮对话，聊着聊着就把前面的内容忘了。

### 1.2 RAG 是"外接记忆卡"

**RAG**（检索增强生成）可以理解为给 AI 插了一张"外接记忆卡"：

```
没有 RAG：AI 只靠自己的脑子（训练数据）回复
有 RAG：AI 先去记忆库里查一下相关资料，再结合查到的东西回复
```

就像你考试的时候：
- **没 RAG**：闭卷考试，全凭记忆力
- **有 RAG**：开卷考试，可以翻笔记

### 1.3 这个项目做了什么

RAG-RPG 记忆引擎做的是：

1. **记下你和 AI 的每一轮对话** — 像写日记一样
2. **把对话变成 AI 能快速搜索的格式** — 像给日记做索引
3. **在 AI 回复前，帮它查一下相关记忆** — 像提醒你"上次说到这儿了"
4. **把查到的东西塞进 AI 的提示词里** — 像在考试前给你一张小抄

最终效果：AI 能记住很久以前的事情，角色扮演不再出戏。

---

## 二、启动引擎（开机流程）

你应该已经部署好了项目，现在只需要按顺序启动两个东西：**引擎** 和 **SillyTavern**。

### 2.1 启动 Python 引擎

**步骤：**

1. 打开 **PowerShell**（按 `Win` 键，输入 `powershell`，回车）
2. 进入项目文件夹：
   ```
   cd D:\Study\Project\rag-rpg
   ```
3. 激活虚拟环境：
   ```
   .\venv\Scripts\activate
   ```
   如果看到行首多了 `(venv)` 就说明成功了。
4. 启动服务：
   ```
   python server.py
   ```

**如果一切顺利**，你会看到类似这样的输出：

```
正在初始化 RAG-RPG 记忆引擎 v2.0 ...
向量库状态: {'character_skills': 10, 'dialogue_memory': 0, ...}
服务已就绪 → http://127.0.0.1:8765
```

**💡 注意：** 这个 PowerShell 窗口不能关，关了引擎就停了。如果你要关掉，AI 的记忆功能也会停止。

---

### 2.2 启动 SillyTavern

打开另一个 PowerShell 窗口（别关引擎那个），正常启动 SillyTavern：

```
cd D:\你的SillyTavern路径
node server.js
```

然后在浏览器中打开 SillyTavern 的网页（通常是 `http://localhost:8000`）。

---

## 三、安装记忆引擎扩展（让两个程序互相认识）

Python 引擎已经启动了，SillyTavern 也跑起来了，现在需要让它们"握手"。

### 3.1 找到扩展文件夹

在 SillyTavern 的安装目录下，找到这个文件夹：

```
SillyTavern\data\default-user\extensions\
```

### 3.2 复制扩展

1. 打开文件资源管理器（按 `Win + E`）
2. 打开第一个窗口，定位到：`D:\Study\Project\rag-rpg\st_extension`
3. 打开第二个窗口，定位到：`SillyTavern\data\default-user\extensions\`
4. 把 `st_extension` 文件夹复制到 `extensions` 目录下
5. **重命名**为 `RAG-RPG`

最终路径看起来应该是这样：

```
SillyTavern\data\default-user\extensions\RAG-RPG\
    ├── manifest.json
    └── index.js
```

### 3.3 在 SillyTavern 中启用

1. 回到 SillyTavern 网页
2. 点击右上角的 **插头图标**（扩展面板）
3. 在列表中找到 **RAG-RPG 记忆引擎**
4. **确认它的开关是打开的**（绿色）

> **搞定！** 现在引擎和 SillyTavern 已经连上了。

---

## 四、开始使用（日常操作）

安装好之后，**你几乎不需要做任何额外操作**，引擎会自动运行。

### 4.1 正常聊天

就像平时一样选择角色卡开始聊天，比如说选择 **"天际省叙事者"**。

**自动发生的事（无需你干预）：**

```
你发送消息
  → 引擎自动记下你的话
  → AI 准备回复
  → 引擎自动查询相关记忆
  → 把记忆注入提示词
  → AI 生成带记忆的回复
```

### 4.2 如何知道引擎在工作？

**看引擎的 PowerShell 窗口**，你会看到不断刷新的日志：

```
[INFO] 已处理 Turn#5 | user | 关键术语: ['光剑精通']
[INFO] 已处理 Turn#6 | ai | 关键术语: ['霜噬之刃', '里·鬼剑术']
```

如果看到这些，说明引擎正在正常工作。

### 4.3 第一次建议：先初始化记忆库

在开始正式玩游戏之前，先让引擎把角色信息加载进去：

```powershell
# 在另一个 PowerShell 中执行
python scripts/ingest_initial.py
```

这条命令会读取角色的技能和设定，存入向量库。这样 AI 一开始就知道角色会什么技能。

---

## 五、进阶操作（按需使用）

下面这些功能不是必须的，但学会了能让体验更好。

### 5.1 查询当前记忆库状态

想知道引擎记住了多少东西？

```powershell
# 在浏览器中打开（或者在 PowerShell 中用 curl）
curl http://127.0.0.1:8765/api/status -H "X-API-Key: rag-rpg-local"
```

会返回类似：
```json
{
  "vector_db": {
    "character_skills": 10,
    "dialogue_memory": 45,
    "my_rag_memory": 0
  }
}
```

- `character_skills: 10` — 技能库有 10 条
- `dialogue_memory: 45` — 已经记录了 45 轮对话

### 5.2 手动添加记忆

有时候你想让 AI 记住一些特别的事情，可以创建 `new_batch.txt` 文件：

打开 `D:\Study\Project\rag-rpg\new_batch.txt`，添加内容：

```
[SKILL] 技能：剑魂觉醒。当角色生命值低于 30% 时，可以触发剑魂觉醒，全属性提升 50%。
[PLOT] 剧情：玩家在寒落神殿地下室发现了一本古老的剑术秘籍。
[SETTING] 设定：白漫城的领主是巴尔古夫，他对龙裔持观望态度。
```

保存文件后执行：

```powershell
python scripts/ingest_new.py
```

### 5.3 搜索记忆库（交互式）

如果你想看看引擎记住了什么，可以打开交互式搜索：

```powershell
python scripts/quick_search.py
```

然后输入问题，比如：

```
🔍 你想查询什么记忆？ > 光剑精通
```

引擎会返回最相关的记忆条目：

```
与「光剑精通」最相关的 3 条记忆：
  1. [skill] 距离:0.1234
     技能：光剑精通。剑魂对光剑的专属掌控能力...
```

### 5.4 调整扩展设置

在 SillyTavern 的扩展面板中，点击 **RAG-RPG 记忆引擎** 的设置按钮，你可以调整：

| 设置项 | 默认值 | 什么时候需要改 |
|--------|--------|----------------|
| API URL | `http://127.0.0.1:8765` | 只有在另一台电脑上运行引擎时才需要改 |
| 自动捕获 | 开启 | 关掉后引擎不再自动记录对话 |
| 自动查询 | 开启 | 关掉后 AI 不再自动查记忆 |
| 查询轮次 | 6 | 改大 = 考虑更多历史；改小 = 更关注当前 |
| 调试模式 | 关闭 | 遇到问题时可以打开，看看有没有报错 |

---

## 六、常见问题排查

### Q1: 引擎启动了，但 AI 回答好像没区别？

**可能原因①：** 记忆库还是空的。
→ **解决：** 跑一下 `python scripts/ingest_initial.py`。

**可能原因②：** 才聊了几句，还没积累记忆。
→ **解决：** 多聊几轮，记忆会慢慢积累。

**可能原因③：** 扩展没安装成功。
→ **解决：** 检查 SillyTavern 扩展面板里 RAG-RPG 是否已经启用。

### Q2: 引擎窗口报错 "Connection refused"

```
Error: Connection refused
```

**原因：** SillyTavern 连不上 Python 引擎。

**解决：**
1. 确认引擎 PowerShell 窗口还开着
2. 确认引擎输出了 "服务已就绪" 字样
3. 确认扩展设置里的 API URL 是 `http://127.0.0.1:8765`

### Q3: 引擎窗口报错 "invalid api key"

**原因：** API Key 不匹配。

**解决：** 引擎的默认 Key 是 `rag-rpg-local`。检查 SillyTavern 扩展设置里的 API Key 是否也是这个。如果你改了 `config.py` 里的 `API_KEY`，两边要一致。

### Q4: 关闭电脑后，之前的记忆还在吗？

**在的。** 记忆存在你的电脑硬盘里（`chroma_db` 文件夹），不会丢失。

下次使用时：
1. 启动引擎：`python server.py`
2. 启动 SillyTavern
3. 继续聊天 → 引擎会自动加载已有的记忆

### Q5: 聊天记录太多会不会变慢？

**不会明显变慢。** 向量搜索引擎（ChromaDB）设计上就能处理海量数据。每次查询只检索最相关的 3 条，速度基本恒定。

### Q6: 记忆出错了，想清空重来？

终止引擎（按 `Ctrl+C`），然后：

```powershell
# 删除向量库（注意：这会清空所有记忆！）
Remove-Item -Recurse -Force chroma_db

# 重新创建向量库
python scripts/ingest_initial.py
```

---

## 七、实用技巧

### 技巧一：多开窗口

推荐打开**三个** PowerShell 窗口：

| 窗口 | 运行内容 | 作用 |
|------|----------|------|
| 窗口 1 | `python server.py` | 引擎（一直开着别关） |
| 窗口 2 | 启动 SillyTavern | 聊天界面 |
| 窗口 3 | 执行各种命令 | 查看状态、导入数据等 |

### 技巧二：熟记常用命令

把下面几条命令贴在记事本里，需要时复制粘贴即可：

```powershell
# 启动引擎
cd D:\Study\Project\rag-rpg
.\venv\Scripts\activate
python server.py

# 初始化记忆库
python scripts/ingest_initial.py

# 查看状态
curl http://127.0.0.1:8765/api/status -H "X-API-Key: rag-rpg-local"
```

### 技巧三：善用扩展设置

在 SillyTavern 扩展面板中，如果把 **Debug Mode（调试模式）** 打开，浏览器控制台会输出详细日志，方便排查问题。

打开方式：
1. 在浏览器中按 `F12`（开发者工具）
2. 切换到 **Console（控制台）** 标签
3. 会看到以 `[RAG-RPG]` 开头的日志

### 技巧四：先试后改

第一次使用时，**保持所有设置为默认值**。先用一段时间，等熟悉了再根据需要调整参数。默认设置已经经过了充分测试。

---

## 八、一句话总结

> **RAG-RPG 记忆引擎 = 给 AI 装了一个不会忘事的外接大脑。**
>
> 启动它，装好扩展，然后正常聊天就行——剩下的自动完成。
>
> 如果遇到问题，先看引擎窗口有没有报错，再看扩展有没有启用，十有八九能解决。

---

*指南版本：v1.0 · 适用于 RAG-RPG v2.0*
